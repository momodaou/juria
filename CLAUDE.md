# JURIA — Contexte du projet

## Description
JURIA est une application de gestion de cabinet d'avocats développée pour **JFC Avocats Mali** (droit OHADA). Elle couvre l'ensemble du cycle de vie du cabinet : dossiers, clients/KYC, procédure et échéances, facturation, gestion administrative/RH, et un assistant IA d'aide à la rédaction.
Dépôt GitHub : https://github.com/momodaou/juria.git

Spécification initiale : démo interactive HTML (`JURIA demo - MAJ 09.08.2026.html`) fournie par l'utilisateur, retranscrite dans `DOC/JURIA - Dossier de Specifications Fonctionnelles - MAJ 16.08.2026.docx`.

### Périmètre fonctionnel (17 modules)
1. **Cockpit** — vue d'ensemble et pilotage du cabinet (KPIs, délais critiques, rentabilité, tâches perso)
2. **Dossiers 360** — liste des dossiers + fiche 360° (parties, délais, pièces, temps, communications, instances)
3. **Nouveau dossier (ouverture)** — contrôle des conflits d'intérêts puis création (référence, lettre de mission, KYC, tâches auto)
4. **Clients & KYC** — registre clients, suivi KYC/LBC-FT, registre des originaux physiques confiés
5. **Échéancier** — délais de procédure par dossier + échéances administratives récurrentes (fiscal, social, ordinal)
6. **Rôle d'audience** — agenda hebdomadaire des audiences, diffusion équipe, planning des diligences, retours d'audience
7. **Registre du courrier** — enregistrement arrivée/départ, référencement auto, GED, déclenchement d'événements
8. **Atelier d'actes** — génération d'actes via modèle GED ou draft IA, visas à jour, en-tête cabinet
9. **Bibliothèque** — jurisprudence, textes OHADA, veille législative, modèles, consultations, checklists
10. **Plan d'action** — suivi des tâches du cabinet en kanban (à faire/en cours/à valider/terminé)
11. **Chrono & Facturation** — édition auto de factures/notes de frais, TVA selon localisation client, multi-devises, suivi des impayés
12. **Dépenses & caisse** — charges du cabinet, débours clients refacturables, petite caisse, vignettes de plaidoirie
13. **Rétrocessions** — calcul et suivi des rétrocessions d'honoraires (Associé 30% / Collab. avocat-stagiaire-Of Counsel 25% / Collab. non-avocat 10%, "tout ou rien" après encaissement intégral), suivi Pro Bono
14. **Accès & permissions** — matrice d'accès (Collaborateur / Associé-Partner / Finance-Direction), délégations temporaires, journal d'audit
15. **Cabinet (RH)** — équipe et charge de travail, congés, alertes RH, pointage, compteur d'heures, option paie légère
16. **Assistant IA** — capacités d'assistance (résumé, extraction de faits, chronologie, analyse contractuelle, traduction, comparaison) — phase à valider
17. **Portail client** — extranet client (dossiers, factures, messagerie) — phase 4

### Acteurs / rôles
**13 statuts réels** (précisés par l'utilisateur le 17/08/2026 en deux temps — 11 initialement, remplaçant l'estimation à 8, puis complétés avec l'associé-fondateur et la distinction avocat stagiaire/stagiaire simple) : Avocat associé, **Avocat associé-fondateur** (chairman, mêmes droits qu'un associé classique partout SAUF la matrice de permissions — volontairement exclu, précision explicite de l'utilisateur), Avocat Of Counsel, Avocat collaborateur, **Avocat stagiaire** (parcours avocat), **Stagiaire** (non-avocat, ex. stage étudiant — distinct du précédent), Collaborateur non-avocat/juriste, Administrateur général, Assistante juridique et administrative, Comptable, Assistant comptable, Administrateur IT, Archiviste — plus Client (via portail, sans authentification séparée, cf. limitation documentée du module Portail client). Mapping technique et système de permissions : voir « Statuts du personnel + permissions configurables » dans l'État actuel ci-dessous.

### Règles métier transverses clés
- Export Excel/CSV + impression PDF (en-tête cabinet) sur chaque écran
- Alertes par délai : J-7 rouge / J-15 orange / au-delà bleu
- Gestion multi-instance (1re instance / appel / cassation-CCJA) sous référence de dossier unique
- FCFA devise pivot, EUR à taux fixe (655,957), autres devises au taux du jour verrouillé à l'émission
- Échéances fiscales/sociales maliennes pré-paramétrées (TVA, INPS, ITS, IS, patente, Ordre des avocats, RC pro)

## Structure du dépôt
- `APP/` — code source de l'application
- `DOC/` — documentation du projet
- `CLAUDE.md` — ce fichier : contexte pour les futures sessions
- `HISTORY.md` — journal chronologique des actions/décisions

## Stack technique (réelle, adoptée le 16/08/2026)

> ⚠️ Cette stack remplace une proposition initiale (Next.js/TypeScript + Prisma) écartée après découverte d'un kit de démarrage déjà préparé par l'utilisateur (`JURIA kit demarrage - MAJ 09.08.2026.zip`), utilisé comme base réelle du projet. Voir `HISTORY.md` (entrée du 16/08/2026, "Pivot stack technique").

### Frontend
- **Angular 22** (standalone components, signals, `@if`/`@for`), TypeScript
- Projet complet dans `APP/frontend/` (scaffoldé via `@angular/cli@22`)
- `mammoth` (docx→HTML) et `xlsx`/SheetJS (xlsx/xls/csv→tableau) pour l'aperçu de fichier côté client (21/08/2026, voir État actuel) — `xlsx` installé depuis le CDN officiel SheetJS (`https://cdn.sheetjs.com/...`), pas le registre npm (versions npm non corrigées d'une vulnérabilité haute connue, sans correctif publié par l'éditeur sur npm).
- ⚠️ **Pas de zone.js** (aucune entrée `polyfills` dans `angular.json`, `zone.js` absent des dépendances directes — confirmé le 04/09/2026 en diagnostiquant un bug réel). Conséquence directe pour tout code futur : après la résolution d'un appel HTTP asynchrone (`.subscribe({ next: ... })`), **muter directement une propriété d'un objet déjà affiché ne déclenche aucun nouveau rendu** — il faut réécrire le signal qui le porte via `.set()`/`.update()` (jamais `objet.propriete = valeur` sur un objet issu d'un signal). Même règle pour un `computed()` : il ne se réévalue que si un **signal** qu'il lit change, jamais une propriété simple modifiée via `[(ngModel)]` (voir le bug du tri du Cockpit, 04/09/2026). Motif de bug déjà rencontré deux fois le même jour (Cockpit, Accès & permissions) — à vérifier systématiquement pour tout nouveau code qui met à jour l'écran depuis un callback asynchrone.

### Backend
- **API Node.js/Express** (JavaScript, pas TypeScript), dans `APP/backend/`
- Auth par JWT (`jsonwebtoken` + `bcryptjs`), pas de framework auth tiers
- Upload fichiers : `multer` ; stockage GED : `@google-cloud/storage`

### Base de données
- **PostgreSQL 15**, schéma SQL géré à la main (`APP/db/schema.sql`, pas d'ORM/Prisma) — 36+ tables (dossiers, clients, facturation, rétrocessions, échéances, RH, etc.)
- Client : `pg` (requêtes SQL directes, pas de query builder)

### Déploiement — Google Cloud Platform
- **Compute** : Cloud Run (conteneur API Node.js)
- **Base** : Cloud SQL for PostgreSQL
- **Stockage** : Google Cloud Storage (GED)
- **Secrets** : Secret Manager
- Script de référence : `JURIA deploiement gcp - MAJ 01.08.2026.sh` (dans les Documentations utilisateur, à intégrer dans le dépôt)
- Domaine cible : `extranet.jfcavocats-mali.com`

### IA
- Assistant IA prévu via Gemini/Vertex AI (`IA_MODEL=gemini-1.5-flash` dans `.env.example`) — `backend/src/ia.js`

### Développement local
- **Docker** pour tout (Node.js n'est pas installé sur la machine de dev actuelle) : `docker compose up --build` (API + Postgres), front lancé via conteneur `node:22` (voir `APP/README.md`)

## Conventions
- Code backend/DB en français fonctionnel (noms de tables/colonnes, ex: `dossiers`, `utilisateurs`, `mot_de_passe`) — cohérent avec le kit existant, à conserver
- Toute décision structurante doit être consignée dans `HISTORY.md`

## État actuel
- Dépôt git initialisé, lié à `origin` (https://github.com/momodaou/juria.git), poussé (branche `main`).
- `APP/` : kit de démarrage intégré et vérifié fonctionnel (16/08/2026) — backend Node/Express + PostgreSQL 15 démarrent via Docker, schéma chargé (36 tables), authentification JWT testée avec succès (login + endpoints protégés `/api/dashboard`, `/api/dossiers`). Frontend Angular 22 scaffoldé et overlayé avec le socle du kit (login, cockpit, dossiers, fiche dossier, ouverture, échéancier, facturation) — build vérifié sans erreur.
- **Déployé sur GCP (projet `jfc-juria`, région `europe-west1`)** :
  - Frontend : https://juria-web-552099340909.europe-west1.run.app
  - API : https://juria-552099340909.europe-west1.run.app
  - Compte de test : `associe@jfcavocats-mali.com` / `DemoPass123!`
  - Ressources : Cloud SQL PostgreSQL (`juria-pg`), bucket GCS (`jfc-juria-ged`), Secret Manager (`juria-db-password`, `juria-jwt-secret`), Artifact Registry (`juria`)
  - `scripts/gcloud-docker.sh` : wrapper pour piloter `gcloud` via Docker (aucun outil GCP installé sur l'hôte)
- **Les 17 modules de la spécification fonctionnelle sont développés** (backend + écran), avec les limitations ci-dessous documentées plutôt que masquées :
  - **Clients & KYC** — registre, fiche 360°, pièces KYC avec alertes d'expiration, originaux confiés
  - **Rôle d'audience** — agenda hebdomadaire, validation/diffusion, retours d'audience avec renvoi automatique au rôle suivant
  - **Registre du courrier** — arrivée/départ, référencement auto, déclenchement automatique d'événements/diligences/tâches
  - **Atelier d'actes** — génération via modèles internes (codés en dur, pas de table) ou brouillon Assistant IA, enregistré dans la GED
  - **Bibliothèque** — jurisprudence/textes/veille/modèles/consultations/checklists, nouvelle table `ressources_biblio`
  - **Plan d'action** — kanban sur la table `taches` déjà prévue par le kit
  - **Dépenses & caisse** — circuit soumise→validée/rejetée→décaissée, petite caisse, vignettes de plaidoirie
  - **Rétrocessions** — calcul par qualité (30/25/10 %), règle « tout ou rien », Pro Bono, nouvelle table `retrocessions`
  - **Accès & permissions** — rôles, délégations, journal d'audit, **création de compte + validation à l'entrée** (17/08/2026 : un compte créé par un associé/admin démarre **inactif** avec un mot de passe temporaire généré côté serveur — affiché une seule fois, jamais journalisé — et ne devient utilisable qu'après une validation explicite distincte d'une simple réactivation ; distinction "en attente"/"suspendu" via la colonne `utilisateurs.valide_le`, voir ci-dessous)
  - **Cabinet/RH** — équipe, congés (nouvelle table `conges`), pointage, échéances RH, bulletins de paie légers
  - **Assistant IA** — 6 capacités (résumé, chronologie, extraction de faits, analyse contractuelle, traduction, comparaison), toutes en `POST /api/ia/*`, garde-fou « projet à valider » systématique
  - **Portail client** — écran d'**aperçu** côté cabinet (pas un vrai extranet client) : simule ce qu'un client verrait pour un dossier (documents, factures) ; **aucune authentification client séparée, aucune messagerie réelle** — délibérément non implémenté (nécessiterait un système d'auth distinct, hors scope de cette session), conforme au statut « phase 4 » du schéma lui-même (commentaire de fin de `schema.sql`)
- **Cycle de vie d'un compte** (17/08/2026) : `POST /api/acces/utilisateurs` crée le compte avec `actif=FALSE` et renvoie un mot de passe temporaire en clair (une seule fois, dans la réponse HTTP — jamais stocké en clair ni journalisé) ; `POST /api/acces/utilisateurs/:id/valider` l'active la première fois (`valide_par`/`valide_le` renseignés, refuse si déjà validé) ; `PUT /api/acces/utilisateurs/:id/actif` reste utilisé pour suspendre/réactiver un compte déjà validé. Trois états dérivés côté UI : `actif=TRUE` → Actif ; `actif=FALSE ET valide_le IS NULL` → en attente de validation ; `actif=FALSE ET valide_le IS NOT NULL` → suspendu.
- **Changement de mot de passe en libre-service** (17/08/2026) : nouvelle route `GET/PUT /api/profil` (`backend/src/routes/profil.js`, montée sans restriction de rôle — accessible à tout utilisateur authentifié, contrairement à `/api/acces`). `PUT /api/profil/mot-de-passe` exige l'ancien mot de passe, ≥8 caractères, différent de l'ancien. Écran `Mon compte` (`pages/mon-compte/`) accessible depuis le menu par tous.
- **Réinitialisation de mot de passe par un admin** (17/08/2026, comble le gap ci-dessus) : `POST /api/acces/utilisateurs/:id/reinitialiser-mot-de-passe` (associé/admin) génère un nouveau mot de passe temporaire pour un compte existant, sans toucher à `actif`/`valide_le` (contrairement à la création, le compte garde son statut — pas de re-validation à faire). Réutilise le même générateur que la création de compte. Bouton « Réinit. mot de passe » sur chaque membre dans l'écran Accès & permissions, réutilise le même bandeau d'affichage unique du mot de passe.
  - ⚠️ **Gap restant** : toujours pas de flux self-service « mot de passe oublié » pour l'utilisateur lui-même (il doit demander à un associé/admin) — acceptable pour un petit cabinet, mais pas d'auto-service depuis l'écran de connexion. Pas d'envoi automatique par e-mail non plus (communication manuelle).
- **Audit du 17/08/2026** (routes + sécurité + infra GCP + CI/CD, rapport complet dans `HISTORY.md`) :
  - Les 5 routes jamais testées (`temps.js`, `evenements.js`, `communications.js`, `documents.js`, `dashboard.js`) ont été auditées : `temps.js`/`evenements.js`/`dashboard.js` sont corrects ; `documents.js` avait le même bug de typage enum que les autres (corrigé, cast explicite `::categorie_document`/`::confidentialite`) ; `communications.js` manque juste une validation d'entrée sur `type` (mineur, non corrigé).
  - Points vérifiés conformes : bucket GCS correctement privé (accès uniforme, aucune liaison publique), invocation publique des services Cloud Run intentionnelle (sécurité déléguée à la couche JWT applicative).
- **Corrections de l'audit appliquées le 17/08/2026 (priorités 1 à 3 sur 5, avec l'accord de l'utilisateur)** :
  - **Comptes de service dédiés** : `juria-api-sa@jfc-juria.iam.gserviceaccount.com` (droits : `roles/cloudsql.client` au niveau projet, `roles/storage.objectAdmin` limité au bucket `jfc-juria-ged`, `roles/secretmanager.secretAccessor` sur `juria-db-password`/`juria-jwt-secret` uniquement) et `juria-web-sa@jfc-juria.iam.gserviceaccount.com` (aucun droit GCP, le frontend n'en a pas besoin). Les deux services Cloud Run ont été basculés dessus (`gcloud run services update --service-account=...`) et re-testés avec succès (santé, connexion, DB, upload GED). Le compte Compute par défaut garde `roles/editor` au niveau projet (non retiré — modification plus large et plus risquée, délibérément laissée de côté) mais **n'est plus utilisé par aucun des deux services**.
  - **Sauvegardes Cloud SQL activées** : quotidiennes à 03h00, 7 conservées, + récupération à un instant T (`pointInTimeRecoveryEnabled: true`).
  - **CORS restreint** : `server.js` n'accepte plus que les origines listées dans `ALLOWED_ORIGINS` (variable d'env, virgules), repli par défaut sur le frontend Cloud Run + `localhost:4200` (dev local).
  - **Limitation de débit sur `/auth/login`** : `express-rate-limit`, 10 tentatives / 15 min / IP, réponse 429 au-delà. Nécessite `app.set("trust proxy", 1)` dans `server.js` (Cloud Run place l'app derrière le proxy front-end de Google — sans ce réglage, `req.ip` renvoie l'IP du proxy pour tout le monde, et la limite se partagerait entre tous les utilisateurs au lieu d'être par IP réelle). Ce réglage profite aussi à la capture d'IP du journal d'audit (`logAudit`), qui utilisait déjà `req.ip`.
  - **Helmet** : `app.use(helmet(...))` avec `contentSecurityPolicy: false` (API JSON pure, la CSP n'a pas d'objet) et `crossOriginResourcePolicy: { policy: "cross-origin" }` (nécessaire car le frontend et l'API sont deux origines Cloud Run distinctes qui échangent des fichiers). Vérifié : en-têtes présents (`X-Frame-Options`, `X-Content-Type-Options`, `Strict-Transport-Security`), téléchargement inter-origines toujours fonctionnel.
  - **Filtrage de type sur les téléversements** : nouveau module `backend/src/uploadFilter.js` (liste blanche : PDF, images, Word/Excel/PowerPoint, texte brut), branché en `fileFilter` sur les 3 points de téléversement (`documents.js`, `clients.js` pièces KYC, `biblio.js`). Un gestionnaire d'erreurs global ajouté dans `server.js` (après le 404) pour que le rejet remonte en JSON propre plutôt qu'en page d'erreur HTML par défaut d'Express.
  - **SSL forcé sur Cloud SQL** (`requireSsl: true`) — sans impact sur Cloud Run (le connecteur Cloud SQL Auth Proxy chiffre déjà le trafic indépendamment de ce réglage, qui vise les connexions directes par IP).
  - **IP publique Cloud SQL non retirée — décision volontaire** : la désactiver proprement demanderait de mettre en place l'accès de service privé (VPC peering) + un connecteur Serverless VPC Access pour Cloud Run (coût récurrent, changement de topologie réseau) puisqu'aucune IP privée n'existe aujourd'hui sur l'instance — la couper sans ce préalable couperait tout accès à la base. Jugé disproportionné pour le gain (l'IP publique n'a aucun réseau autorisé configuré, donc déjà fermée aux connexions directes). Laissé tel quel, à réévaluer si besoin.
  - **CI/CD : tenté puis explicitement abandonné par l'utilisateur** (17/08/2026, voir `HISTORY.md`) — bute sur des modifications IAM bloquées par le classificateur de permissions de l'environnement ; l'utilisateur a décidé de rester sur le déploiement manuel (`gcloud builds submit` + `gcloud run deploy`). **Ne pas relancer ce chantier sans demande explicite.**
- Limitations transverses à connaître :
  - Le journal d'audit (`journal_audit`) n'est alimenté que par la connexion et les actions du module Accès & permissions — le reste de l'application (dossiers, factures, dépenses…) n'y écrit pas (retrofit complet non fait, hors scope)
- Nouvelle route `GET /api/utilisateurs` (annuaire interne, `backend/src/routes/utilisateurs.js`) — à réutiliser pour tout sélecteur de responsable/affectation (RH, permissions…) plutôt que d'en recréer une.
- Note : le schéma SQL ne comportait pas de table dédiée « modèles/bibliothèque » — l'Atelier d'actes utilise des modèles codés en dur côté serveur (`backend/src/routes/actes.js`, objet `MODELES`), distincts de la table `ressources_biblio` (ajoutée le 16/08/2026 pour le module Bibliothèque, catalogue consultable avec fichier optionnel — pas un moteur de fusion de champs).
- ⚠️ Motif récurrent de bug rencontré : dans ce codebase (SQL brut, pas d'ORM), `COALESCE($n, 'litteral')` ou `CASE WHEN $n = 'litteral' THEN…` sur une colonne de type ENUM **ou UUID** Postgres provoque une erreur de typage dès que le paramètre est réutilisé dans un contexte non typé. Toujours caster explicitement (`$n::mon_enum`, `$n::uuid`) ou dupliquer le paramètre. Autre variante rencontrée : un placeholder `$n` numéroté mais jamais réellement utilisé dans le texte SQL (reliquat de refactoring) fait échouer `could not determine data type of parameter $n` — vérifier que chaque `$n` du tableau de paramètres correspond bien à une occurrence dans la requête. Déjà corrigé dans `dossiers.js`, `audiences.js`, `courriers.js`, `taches.js`, `documents.js`, `cabinet.js`. **Audit terminé à 100 % (17/08/2026)** : toutes les routes backend ont été relues/testées, y compris `factures.js` (relu et testé en Docker — pas de bug, l'unique `COALESCE($9,0)` porte sur un numérique, pas une chaîne face à un enum).
- **Suite de tests de non-régression** (17/08/2026, `backend/tests/`) : Jest + Supertest, ciblée précisément sur le motif de bug ci-dessus plutôt qu'une couverture exhaustive (jugé suffisant face à CI/CD lors de l'échange « Est-ce important ? » — voir `HISTORY.md`). `tests/setup.js` crée un utilisateur de test idempotent ; `tests/regression.test.js` couvre 2 tests de connexion + 6 tests POST minimalistes (sans les champs enum/UUID optionnels) sur les 6 routes historiquement buguées. `server.js` exporte désormais `app` sans effet de bord (`app.listen()` gardé par `if (require.main === module)`) pour être importable par supertest. **Validé comme vrai filet de sécurité** : le bug a été réintroduit délibérément dans `taches.js` pour confirmer que le test correspondant vire au rouge (échec confirmé, 400 au lieu de 201), puis le correctif restauré et la suite revérifiée au vert (8/8). Lancement : voir `APP/README.md`. Non lancée automatiquement (pas de CI/CD) — à exécuter manuellement avant un déploiement qui touche aux routes concernées.
- **Multi-devises et TVA par localisation câblés dans la facturation** (17/08/2026, suite à l'échange « qu'en pensez-vous ? ») : en creusant `factures.js` pour écrire des tests financiers, découverte que la règle transverse « FCFA pivot, EUR à 655,957 fixe, autres devises verrouillées à l'émission, TVA selon localisation » n'était **pas implémentée** dans le code, alors que le **schéma la prévoyait déjà** — table `devises` (XOF/EUR à parité fixe, USD/GBP flottantes) et `taux_change` (historique), colonnes `factures.taux_applique/date_taux/taux_verrouille/montant_ttc_xof/libelle_principal`, `paiements.devise/taux_applique/montant_xof`, tout hérité du kit de démarrage dès le premier commit mais jamais câblé. Câblage fait dans `factures.js` :
  - `POST /api/factures` accepte `devise?` (défaut XOF) et `taux_applique?`. Pour une devise à parité fixe (`devises.flottante = FALSE`, ex. EUR), le taux vient de `devises.parite_xof` et **ignore** tout `taux_applique` fourni par l'appelant (parité non négociable). Pour une devise flottante (USD, GBP), soit l'appelant fournit `taux_applique` pour cette facture (archivé dans `taux_change`), soit le dernier taux connu pour cette devise est repris ; sans l'un ou l'autre, la création est **refusée** (`400`) plutôt que d'inventer un taux. Le taux retenu est figé sur la facture (`taux_verrouille=TRUE`) et sert à calculer `montant_ttc_xof` (contre-valeur FCFA, pour le reporting/pivot).
  - `taux_tva` par défaut : 18 % si `clients.pays` est Mali ou vide, 0 % sinon (prestation hors territorialité) — **valeur de départ, pas un avis fiscal**, toujours écrasée si `taux_tva` est fourni explicitement à la création ; à valider avec l'expert-comptable du cabinet avant de s'y fier telle quelle en production réelle.
  - `POST /api/factures/:id/paiements` : un paiement hérite automatiquement de la devise et du taux figés de sa facture (pas de règlement multi-devises sur une même facture — hors scope), et calcule `montant_xof`.
  - Frontend (`pages/facturation/`) : sélecteur de devise (XOF/EUR/USD/GBP), champ taux visible seulement pour les devises flottantes, champ TVA laissé vide par défaut pour appliquer la règle de localisation, affichage de la contre-valeur FCFA dans le tableau des factures.
  - **Tests** : `backend/tests/finance.test.js` (nouveau, 13 tests) — taux par qualité de rétrocession (30/25/10 %), règle « tout ou rien » (refus tant que la facture liée n'est pas intégralement encaissée, acceptation une fois soldée), TVA par défaut selon `clients.pays` et override explicite, XOF sans conversion, EUR à taux fixe avec tentative d'override ignorée, USD sans taux connu refusé, GBP avec taux fourni puis réutilisé sans le refournir, propagation devise/taux sur un paiement. Suite complète (`regression.test.js` + `finance.test.js`) : **21/21 verts**.
  - ⚠️ Piège découvert en testant : le test « devise flottante sans taux connu → refusée » dépend de l'**absence** de tout `taux_change` USD préexistant dans la base — un `docker compose down -v` (avec `-v`) est nécessaire avant de relancer les tests si des factures USD/GBP ont été créées à la main auparavant sur le même volume Postgres local.
- **Supervision production mise en place** (17/08/2026, `APP/monitoring/`) — comblait l'absence totale d'alerte identifiée lors de l'échange « qu'en pensez-vous ? » : canal de notification e-mail (`mohameddaou22@gmail.com`), 2 vérifications de disponibilité (`/health` API, page d'accueil frontend, toutes les 5 min), 3 politiques d'alerte (API indisponible, frontend indisponible, plus de 5 erreurs 5xx API en 5 min — capture les pannes applicatives qu'un `/health` trivial ne verrait pas). Définitions YAML conservées dans le dépôt comme trace/rejouabilité (pas de Terraform sur ce projet, appliqué manuellement via `scripts/gcloud-docker.sh`). ⚠️ Non vérifié avec certitude : si les alertes e-mail nécessitent une confirmation (clic sur un lien reçu par e-mail) avant livraison réelle — à confirmer par l'utilisateur en vérifiant sa boîte mail / la console GCP (Monitoring → Alerting → Notification channels).
- **Statuts du personnel + permissions configurables** (17/08/2026, suite au registre des 53 commandes qui a révélé l'écart entre les 6 rôles techniques et les statuts réels du cabinet) :
  - `role_utilisateur` passe de 6 à **11 valeurs** : `associe` (Avocat associé), `of_counsel` (Avocat Of Counsel), `collaborateur` (Avocat collaborateur), `stagiaire` (Avocat stagiaire), `juriste` (Collaborateur non-avocat/juriste — anciennement fondu dans `assistante`), `admin_general` (Administrateur général — **renommé** depuis `admin`), `assistante` (Assistante juridique et administrative — sens désormais précis), `comptable`, `assistant_comptable` (nouveau), `admin_it` (Administrateur IT, nouveau), `archiviste` (nouveau). Migration en 2 imports Cloud SQL séparés (voir piège ci-dessous).
  - Décisions actées avec l'utilisateur : **Administrateur IT** a les mêmes droits que l'Administrateur général sur toutes les actions métier (identité distincte pour la traçabilité, objectif « débloquer n'importe quelle situation ») ; **Avocat Of Counsel** identique à Avocat collaborateur (cohérent avec la règle de rétrocession qui les regroupe déjà à 25 %) ; **Assistant comptable** consultation/soumission uniquement, jamais décaissement/validation ; **Archiviste** pensé pour GED/courrier/bibliothèque mais pas verrouillé en dur (voir matrice ci-dessous).
  - **Système de permissions configurable** (remplace le câblage en dur `requireRole("associe","admin")` sur les actions métier) : nouvelle table `permissions_role` (role, action_code, autorise — clé composite, absence de ligne = refus par défaut, jamais d'accès implicite), catalogue statique des 44 actions configurables dans `backend/src/permissions.js` (même esprit que `MODELES` dans `actes.js`), middleware `requirePermission(actionCode)` qui a remplacé `requireRole` sur les **44 routes métier** (les 10 déjà réservées + les 34 jusque-là ouvertes à « tous rôles » — céder ce levier à un administrateur était la demande explicite de l'utilisateur, pas juste documenter l'existant). Seedé à la bascule pour reproduire exactement le comportement d'avant.
  - **Garde-fou anti-verrouillage** : le module Accès & permissions lui-même (comptes, rôles, délégations) N'EST PAS dans cette table — reste `requireRole("associe","admin_general","admin_it")` codé en dur, pour qu'une manipulation malheureuse dans la matrice ne puisse jamais retirer à tout le monde le moyen de la corriger.
  - **La matrice elle-même** (`GET/PUT /api/acces/permissions`, écran « Matrice des permissions » dans Accès & permissions) est **plus restreinte que le reste du module** : réservée à Associé + Administrateur IT uniquement (précision explicite de l'utilisateur — l'Administrateur général peut gérer les comptes mais pas modifier qui a le droit de faire quoi). Deux middlewares `requireRole` empilés (ET logique) : le garde commun du routeur laisse passer `admin_general`, le second le rejette spécifiquement sur ces deux routes.
  - `retrocessions.component.ts` : la suggestion auto de qualité par rôle du bénéficiaire mise à jour (`juriste` → 10 %, `of_counsel`/`stagiaire` → 25 % comme `collaborateur`) — sans ce correctif un juriste aurait été suggéré à 25 % au lieu de 10 %.
  - ⚠️ **Piège rencontré en migrant la production** : `ALTER TYPE ... ADD VALUE` suivi de son utilisation (dans `enum_range()`) au sein du **même fichier** importé via `gcloud sql import sql` échoue (`ERROR_RDBMS`), même en séparant les instructions par des points-virgules hors de toute transaction explicite dans le fichier — l'outil d'import Cloud SQL semble empaqueter tout le script importé dans une transaction unique côté serveur, contrairement à `psql -f` en autocommit. **Solution qui a fonctionné** : scinder en deux fichiers/imports Cloud SQL strictement séquentiels — (1) les `ALTER TYPE`, (2) la `CREATE TABLE` + le seed qui utilise les nouvelles valeurs — chaque `gcloud sql import sql` étant une opération/session distincte, la deuxième ne démarre qu'une fois la première pleinement `done`. À réappliquer si un futur changement de schéma ajoute une valeur d'enum réutilisée dans le même lot.
  - Vérifié en local (Docker, schéma neuf) et en production : comptes créés/validés pour `archiviste` et `admin_it`, bascule d'une permission en direct (désactivation de `clients.creer` pour `archiviste` → 403 immédiat, réactivation → 201 de nouveau), `admin_general` confirmé exclu de la matrice (403 sur GET et PUT) tout en gardant le reste du module Accès & permissions (200 sur `/audit`). Suite de tests (`regression.test.js` + `finance.test.js`, 21 tests, aucun ne référence le rôle `admin` littéral) toujours verte après la bascule.
  - Registre des commandes (artefact publié précédemment) mis à jour au même lien : 11 statuts, mention explicite que les valeurs affichées sont modifiables via la matrice et peuvent ne plus refléter l'état courant.
  - **Complément 17/08/2026 (après coup, statuts omis initialement)** : `role_utilisateur` étendu de 11 à **13 valeurs** — ajout de `associe_fondateur` (Avocat associé-fondateur, « chairman ») et scission de l'ancien `stagiaire` en `avocat_stagiaire` (RENAME VALUE, parcours avocat, conserve exactement les permissions déjà en place) + nouveau `stagiaire` (sens redéfini : stagiaire non-avocat).
    - `associe_fondateur` reprend **exactement** les permissions actuelles d'`associe` sur les 46 actions cataloguées (`INSERT ... SELECT ... FROM permissions_role WHERE role='associe'`, plutôt que ré-énumérer à la main) — mais **volontairement exclu** de la garde `requireAssocieOuAdminIt` (routes `/api/acces/permissions`) : l'utilisateur a explicitement corrigé une première proposition qui l'y incluait, en précisant qu'il garde tous les droits d'un associé classique **sauf** la matrice elle-même, qui reste Associé (classique) + Administrateur IT uniquement.
    - Nouveau `stagiaire` (non-avocat) démarre avec le même profil que les autres statuts non privilégiés (copié depuis `archiviste` : actions déjà ouvertes autorisées, rien sur les 10 actions réservées à la direction).
    - Rétrocessions (`retrocessions.component.ts`) : `associe_fondateur` → qualité « associe » (30 %) ; `avocat_stagiaire` → qualité « collaborateur » (25 %, comportement inchangé, juste la clé renommée) ; nouveau `stagiaire` → qualité « non_avocat » (10 %).
    - Migration production appliquée en 2 imports Cloud SQL séquentiels (même piège que la première fois : `ALTER TYPE RENAME/ADD VALUE` ne peut pas être utilisé dans la même transaction qui l'a modifié — voir l'entrée précédente).
    - Vérifié en local (schéma neuf, 21/21 tests, comptes de test `associe_fondateur` et `stagiaire` créés et testés) et en production (compte `associe_fondateur` réel : 200 sur la gestion des comptes, 403 sur la matrice — confirmé).
    - ⚠️ **Correctif 29/08/2026** : cette vérification du 17/08 était incomplète — les 2 routes testées (gestion des comptes, matrice) sont toutes deux verrouillées en dur (`requireRole`), **indépendantes** de la copie en masse `permissions_role` ci-dessus, qui n'avait en réalité **jamais pris en production** (probablement lancée avant que `associe` lui-même ait toutes ses actions actuelles). Trouvé en préparant une matrice de référence « qui voit quoi » à la demande de l'utilisateur : `associe_fondateur` n'avait que **16/62** actions accordées (contre 63/63 pour `associe`) — impossible de créer un dossier, un client, une facture, d'utiliser l'Assistant IA, etc. Corrigé en rejouant la copie via 47 appels à `PUT /api/acces/permissions` (authentifié, chaque changement tracé dans `journal_audit` — pas de migration SQL brute) : `associe_fondateur` est maintenant à 63/63, identique à `associe`.
    - ⚠️ **Correction de politique 29/08/2026 (même jour, session suivante)** : l'utilisateur revient sur la décision du 17/08 — le module **Accès & permissions dans son ensemble** (comptes, délégations, journal d'audit), pas seulement la Matrice, doit désormais être hors d'atteinte pour `associe_fondateur`. `backend/src/routes/acces.js` : garde commune resserrée à `requireRole("associe","admin_general","admin_it")` (associe_fondateur retiré). Frontend : nouveau champ `NavItem.rolesAutorises` (distinct de `requiert`, qui ne couvre que les actions `permissions_role` — ici la garde est un `requireRole` en dur, pas une action cataloguée), entrée « Accès & permissions » masquée du menu pour qui n'est pas dans ce cluster. Nouveau test `accesGarde.test.js` (135/135 au total). Les actions hors de ce module qu'il avait reçues juste avant (`parametres.cabinet.modifier`/`parametres.honoraires.modifier`, routées par `/api/parametres`, pas `/api/acces`) restent inchangées — seul le module Accès & permissions lui-même est concerné par ce resserrement.
- **Role level security — visibilité des listes sensibles (18/08/2026)** : à la suite de questions de l'utilisateur sur le RLS, constat que la matrice de permissions ne portait jusque-là que sur les actions de création/modification, jamais sur le simple fait de VOIR une liste — `GET /api/factures`, `/api/depenses`, `/api/retrocessions` étaient ouvertes à tout rôle ayant l'accès de base au module (donc la rémunération individuelle de chacun visible par n'importe qui) ; `GET /api/cabinet/bulletins?utilisateur_id=` se limitait « à soi-même » par défaut mais était trivialement contournable via le paramètre de requête. Deux axes distingués avec l'utilisateur :
  - **Axe A** (confidentialité fine, dossier par dossier, élément par élément, configurable par profil) : jugé complexe et pas encore mûr — **mis de côté volontairement**, à reprendre plus tard si le besoin se confirme.
  - **Axe B** (modules entiers invisibles/lecture seule pour certains profils : facturation, rétrocessions, salaires, dépenses & caisse) : **implémenté**. 4 nouvelles actions « `.consulter` » dans le catalogue de permissions (`factures.consulter`, `depenses.consulter`, `retrocessions.consulter`, `cabinet.bulletins.consulter`), seedées par défaut à Associé/Associé-fondateur/Administrateur général/Comptable/Assistant comptable + Administrateur IT sur factures/dépenses (**mais pas** sur rétrocessions/bulletins — exclusion délibérée : l'IT a un rôle technique, pas de supervision financière ; s'il a un besoin ponctuel de dépannage, il peut se l'accorder lui-même via la matrice, qu'il a le droit de modifier, puis le retirer).
  - Distinction posée avec l'utilisateur entre deux natures de données : **factures/dépenses** = données financières du cabinet, pas de notion de « propriétaire » → module entier invisible sans exception si non autorisé (`requirePermission("factures.consulter")`/`depenses.consulter` en garde directe sur `GET /`). **Rétrocessions/bulletins de paie** = rémunération individuelle → chacun voit toujours **les siens** sans permission particulière (vérification inline `beneficiaire_id === req.user.sub` / `utilisateur_id === req.user.sub`), seule la vue sur **ceux des autres** exige la permission — corrige au passage le contournement `?utilisateur_id=` des bulletins.
  - `GET /api/profil` étendu pour renvoyer la liste des `action_code` autorisés pour le rôle de l'appelant (`permissions: string[]`) — le frontend ne devine plus et n'essaie plus un appel pour voir s'il échoue : `AuthService.chargerProfil()` (nouveau, appelé après connexion et au démarrage de l'appli si un jeton existe déjà) peuple un signal `permissions` consulté via `auth.peut(actionCode)`. Menu latéral : les entrées Facturation/Rétrocessions/Dépenses & caisse disparaissent silencieusement (pas de message d'erreur) si le rôle n'a pas la permission de consultation correspondante — nouveau champ optionnel `requiert?: string` sur `NavItem`.
  - Vérifié en local (21/21 tests, comptes de test collaborateur/admin_it) et en production : un collaborateur reçoit 403 sur la liste des factures mais 200 sur ses propres rétrocessions ; un admin_it reçoit 200 sur factures/dépenses mais 403 sur la liste complète des rétrocessions (asymétrie voulue confirmée).
- **Gestion de la concurrence (chevauchement entre utilisateurs)** (17/08/2026, suite à une question directe de l'utilisateur) — état des lieux honnête d'abord : JURIA n'a **aucune collaboration temps réel** (pas de WebSocket, pas d'indicateur de présence). Le motif `COALESCE($n, colonne)` déjà utilisé partout protège nativement le cas « deux personnes modifient des **champs différents** de la même fiche en même temps » (chaque colonne non envoyée relit sa valeur courante en base au moment de l'exécution, pas une copie figée côté client) — mais pas le cas « même champ, même moment » (dernier arrivé écrase silencieusement). Deux correctifs appliqués :
  - **Garde contre le double déclenchement d'une transition d'état irréversible** : généralisée à **4 routes qui ne l'avaient pas** (trouvé en auditant les 10 actions de transition à la suite de la question) — `retrocessions.decaisser` (`AND statut <> 'decaissee'`), `taches.valider` (`AND statut <> 'termine'`), `conflicts.decision` (`AND decision = 'en_attente'`), `cabinet.conges.decision` (`AND statut = 'demande'`). Chacune renvoie désormais `409` avec un message explicite (« déjà décaissée/validée/traité(e) par quelqu'un d'autre entre-temps ? ») au lieu d'écraser silencieusement `decaisse_par`/`valide_par`/`decide_par`/`approuve_par`. Les 4 routes qui avaient déjà cette garde (`audiences.role.valider/diffuser`, `depenses.decision/decaisser`, `acces.valider`) n'ont pas été touchées. `cabinet.bulletins` volontairement laissé en upsert (`ON CONFLICT DO UPDATE`) — régénérer/corriger un bulletin du même mois est un usage légitime, pas un bug.
  - **Verrouillage optimiste sur `PUT /api/clients/:id`** (seule route d'édition multi-champs d'une fiche existante — `dossiers.js` n'a pas de PUT du tout) : body optionnel `maj_le_attendu` (valeur de `clients.maj_le` vue au chargement) ; si fourni et périmé, `409` avec message clair plutôt qu'un écrasement muet. ⚠️ Piège rencontré et corrigé : une comparaison stricte `maj_le = $17` échouait presque toujours, y compris pour une écriture légitime immédiatement après le chargement — `TIMESTAMPTZ` est stocké à la microseconde en Postgres, mais un aller-retour JSON/JS Date arrondit à la milliseconde ; la comparaison doit tronquer les deux côtés (`date_trunc('milliseconds', maj_le) = date_trunc('milliseconds', $17::timestamptz)`). Sans `maj_le_attendu` fourni, comportement historique inchangé (pas de vérification). Frontend (`client-detail.component.ts`) mis à jour pour l'envoyer et gérer le `409` en rechargeant automatiquement la fiche.
  - Numérotation automatique (référence courrier, numéro dossier/facture) : fenêtre de course théorique sur le calcul `COUNT(*)+1`, mais une contrainte `UNIQUE` en base empêche tout doublon réel — au pire la requête concurrente perdante échoue avec une erreur brute (pas encore rendue conviviale, noté mais non traité).
  - Volontairement non fait (jugé disproportionné pour la taille du cabinet sauf besoin exprimé) : présence en temps réel, verrouillage pessimiste explicite, rafraîchissement automatique des écrans collaboratifs (rôle d'audience, plan d'action kanban), historique de versions.
  - Vérifié en local (21/21 tests, + tests manuels ciblés sur chacune des 5 corrections : double décaissement, double validation, double décision de conflit, double décision de congé, verrou optimiste client avec les 3 cas — bonne valeur/valeur périmée/sans verrou) et en production (double validation de tâche confirmée : 200 puis 409).
- **Messagerie instantanée interne** (17/08/2026, module 18, hors des 17 initiaux) : demandée par l'utilisateur pour dialoguer entre deux ou plusieurs personnes (échanges dossier, instructions, informations diverses), référence donnée = le chat écrit de Zoom. Distincte de `communications.js` (journal de traçabilité d'un dossier, pas un canal de discussion).
  - **Choix d'architecture déterminant** : JURIA tourne sur Cloud Run (sans état, potentiellement plusieurs instances) — un vrai chat « instantané » demande normalement un composant de diffusion inter-instances (Redis/Firestore/Pub-Sub). Utilisé **Postgres LISTEN/NOTIFY** à la place : une notification émise par une instance est reçue par toutes les connexions en écoute, quelle que soit l'instance qui les détient — pas de service GCP supplémentaire, messages gardés dans la même base sauvegardée/auditée que le reste (pas de fragmentation des échanges professionnels dans un second datastore, point jugé important pour un cabinet d'avocats). Diffusion côté client via **Server-Sent Events** (`EventSource`, auto-reconnexion native du navigateur) plutôt qu'un WebSocket bidirectionnel — l'envoi reste un `POST` classique, seule la réception a besoin d'un canal poussé.
  - **Schéma** : `conversations` (titre optionnel, `dossier_id` optionnel), `conversation_participants` (PK composite, `dernier_lu_le` pour le calcul des non-lus), `messages`. 2 nouvelles entrées dans le catalogue de permissions (`messagerie.creer_conversation`, `messagerie.envoyer_message`), ouvertes à tous les 11 rôles par défaut.
  - **Backend** : `backend/src/messagerie-bus.js` (nouveau) — connexion PG dédiée `LISTEN` (hors du pool `db.js`, avec reconnexion auto sur coupure) + registre en mémoire des flux SSE ouverts localement ; `pousserLocalement()` n'écrit que sur les flux de CETTE instance, la diffusion inter-instances passe uniquement par `NOTIFY`/`pg_notify()`. `backend/src/routes/messagerie.js` (nouveau) : `GET/POST /conversations`, `GET/POST /conversations/:id/messages`, `POST /conversations/:id/lu`, `GET /non-lus`, `GET /stream` (SSE). `/stream` ne peut pas passer par le middleware `authenticate` standard (`EventSource` ne pose pas d'en-tête `Authorization`) — jeton accepté en query (`?token=`), vérifié à la main avec le même `SECRET`.
  - ⚠️ **Piège rencontré et corrigé** : la connexion `LISTEN` dédiée, démarrée au chargement du module, empêchait la suite de tests Jest de se terminer (process qui ne rend jamais la main, timeout au lieu d'un résultat) — corrigé en sautant `demarrerEcoute()` quand `NODE_ENV=test` (positionné automatiquement par Jest). Sans ce garde-fou, toute future suite de tests important ce module se bloquerait silencieusement.
  - **Frontend** : `core/messagerie.service.ts` (nouveau, `providedIn: 'root'`) — un seul `EventSource` partagé pour toute l'application (ouvert une fois après connexion dans `login.component.ts`, fermé à la déconnexion dans `app.component.ts`), pas une connexion par écran ; alimente à la fois la pastille de non-lus de la barre latérale et l'écran dédié. Pas d'ajout optimiste du message envoyé côté client : il revient via son propre écho SSE (l'auteur est lui-même abonné en tant que participant), évite un doublon. `pages/messagerie/messagerie.component.ts` (nouveau).
  - **Déploiement** : délai de requête Cloud Run du service `juria` porté à 3600s (`--timeout=3600`) pour que le flux SSE ne soit pas coupé toutes les 5 minutes (le navigateur reconnecterait de toute façon via `EventSource`, mais c'est plus fluide).
  - Vérifié en local (21/21 tests toujours verts après le correctif `NODE_ENV`, test SSE réel via `curl -N` confirmant la réception en quelques centaines de ms, compteurs de non-lus testés dans les deux sens) et **en production réelle** : message envoyé par un compte, reçu en direct via SSE par un second compte à travers deux requêtes HTTP distinctes (donc potentiellement deux instances Cloud Run différentes) — confirme que LISTEN/NOTIFY diffuse correctement au-delà d'une seule instance, pas seulement en local à instance unique.
- **Seuil minimum d'honoraires par dossier (anti-dissimulation, 18/08/2026)** : demandé par l'utilisateur pour qu'aucun dossier ouvert (hors pro bono) ne puisse rester sans honoraires facturés — risque visé : un avocat apporteur qui traite un dossier sans jamais le faire apparaître financièrement dans JURIA. Deux mécanismes distincts, pas un seul :
  - **Dossier pro bono** (`dossiers.pro_bono`, colonne existant depuis le 17/08/2026 mais jusqu'ici jamais réglable — colonne morte, corrigé ici) : minimum de **50 000 FCFA** de frais de procédure, et surtout un **quota mensuel/responsable** (2 par défaut, anciennement une constante en dur purement informative dans `retrocessions.js`) désormais **réellement bloquant** à la création (`409` au-delà) — sans ce blocage, marquer un dossier « pro bono » aurait été une échappatoire triviale au seuil classique.
  - **Dossier classique** : minimum de **150 000 FCFA d'honoraires** — un plancher, pas un forfait imposé ni un plafond (un dossier peut toujours être facturé bien au-delà). Statut calculé (pas stocké) sur `GET /api/dossiers` et `GET /api/dossiers/:id` : cumul des `factures.montant_ttc_xof` non annulées du dossier comparé au seuil applicable, exprimé nativement en XOF (pas de reconversion à faire, couvre le « ou son équivalent en devise » demandé). `mode_honoraires='abonnement'` exempté (pas de table contrat-cadre dans le schéma pour vérifier au bon niveau — limitation documentée).
  - **Les 3 seuils sont configurables en base** (`parametres_cabinet.honoraires_min_xof/frais_procedure_pro_bono_min_xof/quota_pro_bono_mensuel`), même logique que la matrice de permissions — nouvelle route `GET/PUT /api/parametres/honoraires` (`backend/src/routes/parametres.js`), écriture réservée par une action cataloguée dédiée (`parametres.honoraires.modifier`, seedée associé + admin IT) plutôt qu'un `requireRole` en dur — pas de risque d'auto-verrouillage analogue à la matrice elle-même.
  - **Alertes échelonnées** (J+3 responsable seul, J+7 + associés actifs, J+15 + comptabilité/admin — pas de notion d'« apporteur » distincte de `responsable_id` dans le schéma, limitation documentée) : nouveau job pur `backend/src/jobs/alertesHonoraires.js`, idempotent via 3 colonnes booléennes sur `dossiers` (même patron que `evenements.alerte_j30` etc.), persisté dans une nouvelle table dédiée `alertes_honoraires` plutôt que dans la messagerie interne (`messages.auteur_id` est `NOT NULL`, pensée pour le chat humain, pas les notifications système) — `bus.publier()` réutilisé uniquement pour la poussée SSE temps réel.
  - **Gap préexistant découvert et corrigé au passage** : le job d'alerte de délais existant (`evenements.js`, `POST /jobs/alertes`) n'était **jamais réellement déclenché en production** (aucun Cloud Scheduler configuré dans le projet) et n'avait **aucun contrôle de permission** dessus. Logique extraite dans `backend/src/jobs/alertesDelais.js` (réutilisable), route manuelle désormais protégée par `requirePermission("evenements.jobs.declencher")`, et nouveau point d'entrée dédié `backend/src/routes/internal-jobs.js` (`/internal/jobs/alertes-delais` et `/internal/jobs/alertes-honoraires`) monté **hors** `authenticate` standard — protégé par un secret partagé (`X-Scheduler-Secret` vs `SCHEDULER_SECRET`, refuse `503` si non configuré plutôt que d'accepter tout appel par défaut), même principe que `/api/messagerie/stream` qui gère déjà sa propre authentification hors du middleware standard. ⚠️ **Code prêt, déploiement GCP pas encore fait** à ce stade (secret en Secret Manager + 2 jobs Cloud Scheduler à créer) — à faire avec confirmation explicite avant toute action sur l'infrastructure de production.
  - **Décision de périmètre actée avec l'utilisateur** : pas de blocage des actions du dossier en aval (actes, audiences, courrier) dans cette passe — la configurabilité des seuils sert de soupape (« afin d'éviter des blocages », mot de l'utilisateur) plutôt qu'un verrou dur ; à reconsidérer si l'usage réel montre que visibilité + alertes ne suffit pas.
  - ⚠️ **Gap trouvé en implémentant, non comblé (signalé plutôt que masqué)** : aucun écran frontend ne permet de créer un dossier — `ouverture.component.ts` ne couvre que le contrôle des conflits, `api.creerDossier()` n'est appelé nulle part dans le frontend. La case à cocher « pro bono » prévue au départ n'a donc pour l'instant nulle part où s'accrocher côté UI ; l'API et les tests couvrent `pro_bono` intégralement, un formulaire de création de dossier reste à construire (hors périmètre de cette session).
  - Frontend : badge honoraires sur le rôle des dossiers (`dossiers.component.ts`), panneau honoraires sur la fiche dossier (`dossier-detail.component.ts`), section « Seuils honoraires » dans Accès & permissions (`acces.component.ts`, visible seulement si `auth.peut('parametres.honoraires.modifier')`), nouvelle tuile KPI Cockpit (`dossiers_sous_seuil_honoraires`).
  - Vérifié en local (Docker, schéma neuf) : suite complète **29/29** verte (8 nouveaux tests, `backend/tests/honoraires.test.js`), build Angular production sans erreur, et tests manuels bout en bout (création pro bono 403 sans permission puis 201 avec, quota bloqué au 3ᵉ dossier du mois avec `409`, statut honoraires vérifié sur le rôle, config lecture ouverte/écriture protégée, job délais protégé par permission, point d'entrée interne fermé sans secret `503` / rejeté avec un mauvais secret `401` / fonctionnel et idempotent avec le bon secret).
- **Écran de création de dossier construit + référencement conforme au Guide JFC Avocats (18-19/08/2026, 8 commits, détail complet dans `HISTORY.md`)** : comble le gap précédemment documenté ci-dessus (« aucun écran frontend ne permet de créer un dossier »).
  - `ouverture.component.ts` gagne une étape 2 après contrôle des conflits (client — avec création à la volée si absent —, pôle, matière, juridiction, montant du litige, mode d'honoraires, urgence, responsable, pro bono si habilité), avec champs conditionnels conseil/contentieux (statut de procédure, degré d'instance, intermédiaire côté conseil). `PUT /api/dossiers/:id` ajouté (n'existait pas — aucune modification possible après création), avec le même verrou optimiste que `PUT /api/clients/:id`. `DELETE /api/dossiers/:id`/`clients/:id` ajoutés, limités aux fiches « à l'ouverture » sans activité réelle (409 sinon, archivage recommandé à la place).
  - Un dossier peut avoir plusieurs clients liés (`dossier_clients_additionnels`) et plusieurs instances (table `instances`, déjà en base mais jamais branchée avant cette passe — `POST/PUT /api/dossiers/:id/instances`).
  - **Référencement automatique conforme au *Guide de référencement des dossiers* (JFC Avocats, fourni par l'utilisateur)** : `[TYPE]-[MATIÈRE]-[ANNÉE]-[N° d'ordre]` (ex. `CX-CIV-2026-0048`), TYPE = CS (conseil)/CX (contentieux), compteur remis à 0001 chaque année par type+matière (via `LIKE` incluant l'année, pas de table de compteur dédiée). Remplace une formule provisoire (`AFF-AA-XXX`) introduite dans les tout premiers commits de cette même passe puis jugée non conforme par l'utilisateur. Découverte déterminante : `codes_matiere` (codes + couleur de chemise par pôle) existait en base depuis le tout début du schéma, seedée, mais jamais branchée à aucune route — branchée ici, pas réinventée. `dossiers.code_matiere` (FK composite vers `codes_matiere`) porte la valeur qui compte pour la référence et la couleur ; `dossiers.matiere` (texte libre) reste pour l'affichage. Code `IND` (« indéterminée — à qualifier ») en repli si aucune matière choisie (repli explicite du guide) ; **seule** une requalification depuis `IND` régénère le `numero` par la suite — sinon la référence reste stable/immuable comme l'exige le guide. Nouvelle route `GET /api/dossiers/meta/codes-matiere?pole=`.
  - Widget flottant de messagerie (`app.component.ts`, bulle persistante à travers la navigation) et abandon du seuil d'honoraires minimum classique (150 000 FCFA, décision explicite de l'utilisateur — seul le volet pro bono subsiste) livrés dans la même passe.
  - 50/50 tests verts en fin de passe, build Angular OK, vérifié en Docker.
  - ⚠️ Ces 8 commits (session `session_011DoUuc2sbAV8m9e4fmrgke`) avaient été déployés en production **sans mise à jour correspondante de ce fichier ni de `HISTORY.md`** — comblé après coup le même jour dans une session distincte (voir entrée suivante). Rappel de la convention du projet (section « Notes pour les futures sessions » en bas de ce fichier) : documenter au fil de l'eau, pas après coup.
- **Nettoyage des données de production suite à la découverte du référencement (19/08/2026, session distincte de celle ci-dessus)** : l'utilisateur a observé que les références affichées dans JURIA ne respectaient pas la formule — vérifié en direct sur l'API de production (connexion réelle, pas de suppositions) que le correctif était bien déployé, mais que 7 des 8 dossiers en base dataient d'avant son déploiement. Nettoyage fait exclusivement via l'API authentifiée (jamais de SQL brut) : 5 dossiers de test sans activité supprimés, 1 avec documents réels archivé (suppression refusée par le garde-fou anti-perte de données, comportement voulu), 1 avec facture réelle liée corrigé vers le format conforme via le seul mécanisme prévu par l'appli (requalification `IND` → matière réelle). Détail complet et note sur le déblocage du classificateur de permissions local (`.claude/settings.local.json`) dans `HISTORY.md`.
- **En-tête de la fiche dossier — titre composé (20/08/2026)**, après diagnostic + décisions utilisateur via questions à choix, puis un retour en arrière partiel le même jour une fois le résultat vu en production. État retenu : structure d'en-tête **inchangée par rapport à l'originale** (ordre, badges, grille), seul le titre change — `titrePrincipal(d)` compose désormais `{client(s)} c/ {partie(s) adverse(s)}` (ex. « BDM c/ Arouna Modi COULIBALY ») depuis les données déjà structurées (`client_nom`/`clients_additionnels`/`dossier_parties` rôle adverse) au lieu d'afficher `intitule` ; repli sur `intitule` si aucun client n'est résolu. `objet` (nature de la procédure) reste affiché en sous-titre sous le titre. **Abandonné le même jour, sur décision explicite de l'utilisateur** : le badge de statut de facturation (Pro bono/Convention-Abonnement/Facturé/Non facturé), et toute l'hypothèse pénale MP/parties civiles (« MP c/ XXX (PC : ...) ») — code retiré de `dossiers.js`/`ouverture.component.ts`/`dossier-detail.component.ts`. ⚠️ Les valeurs d'énumération `role_partie.ministere_public`/`partie_civile` **restent en base** (déjà migrées en production, non réversible proprement) mais ne sont plus utilisées par aucun code — même principe que `listes_valeurs('matiere')` désactivé le 19/08. **Gap signalé, toujours non traité** : aucune route pour ajouter des parties après la création (seulement à l'ouverture) ; la liste « Dossiers » affiche toujours `intitule`, pas la ligne composée (portée volontairement limitée à la fiche détail). Build Angular + 50/50 tests backend vérifiés après le retour en arrière.
  - ⚠️ **Titre composé abandonné à son tour, plus tard le même jour** (nouvelle demande utilisateur, session distincte — voir entrée `HISTORY.md` « Retour en arrière ciblé ») : `titrePrincipal(d)` retiré, le titre de la fiche dossier redevient `d.intitule` comme avant toute cette refonte. Seul le sous-titre `objet` (`@if (d.objet)`) subsiste de tout ce chantier — c'est la seule trace encore visible de la refonte du 20/08 dans l'en-tête actuel.
- **Client principal éditable + création de client à la volée en modification de dossier (20/08/2026)** : gap comblé dans le formulaire de dossier (« la matrice » dans la demande initiale de l'utilisateur, clarifié comme le formulaire d'ouverture/fiche dossier, pas la matrice de permissions) — jusqu'ici le client principal n'était éditable qu'à la création, figé ensuite (`PUT /api/dossiers/:id` ne touchait pas `client_id`), et l'ajout de client additionnel en édition ne permettait de choisir que dans les clients déjà existants (pas de création inline, contrairement à l'ouverture). `client_id` ajouté aux colonnes modifiables du `PUT`, et le patron de création à la volée de `ouverture.component.ts` (`creerClientInline`) repris dans `dossier-detail.component.ts`, mutualisé pour le client principal (panneau « Modifier la fiche ») et les clients additionnels (panneau « Clients associés au dossier », déjà visible hors mode édition). Aucune nouvelle permission (`clients.creer`/`dossiers.modifier`/`dossiers.clients_additionnels.gerer` déjà en place). Détail complet dans `HISTORY.md`. **Déployé en production le 20/08/2026** (voir plus bas, déploiement groupé).
- **Rectification de partie adverse + confirmation KYC (20/08/2026)** : nouvelle action cataloguée `dossiers.parties.gerer` (ouverte à tous par défaut) + routes `POST/PUT/DELETE /api/dossiers/:id/parties(/:partieId)` — jusqu'ici `dossier_parties` n'était alimentée qu'à l'ouverture, aucune correction possible ensuite. Le nom du client principal (fiche dossier, section « Clients associés ») est désormais aussi un lien vers `/clients/:id` pour corriger un nom mal saisi, sans dupliquer de formulaire d'identité dans la fiche dossier (garde Clients & KYC comme registre maître, principe posé le 19/08). **Confirmé, pas supposé** : corriger le nom d'un client via ce lien passe par `PUT /api/clients/:id`, qui ne touche `kyc_statut`/`kyc_maj_le` que si ces champs sont explicitement envoyés (jamais le cas ici) — vérifié en local, aucun effet sur le KYC. Rectifier une partie adverse n'a KYC aucun effet par construction (`dossier_parties` n'a aucune colonne KYC, aucun lien vers `clients`/`client_pieces_kyc`). ⚠️ Point de vigilance signalé (pas un gap technique introduit ici, déjà vrai avant cette session) : rien ne distingue automatiquement une correction de faute de frappe d'une substitution d'identité déguisée sur un client déjà KYC-vérifié — question d'habilitation humaine (`clients.modifier`), pas de contrôle système. Détail complet dans `HISTORY.md`. **Déployé en production le 20/08/2026** (voir plus bas, déploiement groupé — inclut la migration de la permission `dossiers.parties.gerer`).
- **Diagnostic Dossiers/Nouveau dossier/Clients & KYC + correctifs (20/08/2026)** : revue de code demandée par l'utilisateur (« beaucoup d'imperfections »), 11 gaps identifiés et classés par impact (détail complet dans `HISTORY.md`). 4 traités cette session :
  - **Contrôle de doublon client avant création** (`GET /api/clients/verifier-doublon?...`) : signale une correspondance RCCM/NIF/nom déjà en base **sans bloquer** (aucune contrainte d'unicité n'existe sur `rccm`/`nif`, et `clients.reference`, prévue `UNIQUE`, n'est alimentée par aucune route — colonne morte, non traitée). Câblé sur les 3 écrans de création de client (registre, création à la volée à l'ouverture et en fiche dossier) avec confirmation explicite « Créer quand même ».
  - **`client_liens` (personnes/entités liées, LBC-FT) enfin accessible** : table présente depuis le tout premier schéma, déjà lue par `GET /api/clients/:id` mais sans aucune route d'écriture ni écran — comblé par `POST/DELETE /api/clients/:id/liens` (gardé par `clients.modifier`) + section dédiée dans `client-detail.component.ts`, avec visibilité bidirectionnelle (`liens_inverses`, un lien saisi depuis A est désormais aussi visible sur la fiche de B).
  - **Rôle des parties rectifiable** : `PUT /api/dossiers/:id/parties/:partieId` acceptait déjà denomination/conseil mais pas `role` (figé à `adverse` depuis la création) — sélecteur des 5 rôles légitimes ajouté (`dossiers.js` cast `::role_partie`, section renommée « Parties au dossier »).
  - **Confirmations manquantes** ajoutées sur les suppressions rapides (client additionnel/partie de dossier, pièce KYC) — jusqu'ici immédiates au clic, sans confirmation ni retour d'erreur.
  - Vérifié : suite de tests backend étendue à 56/56 (schéma neuf), build Angular OK, test manuel bout en bout sur instance Docker complète (RCCM détecté, lien bidirectionnel confirmé, retrait depuis l'autre bout du lien fonctionnel). **Déployé en production le 20/08/2026** (même lot que les 3 entrées précédentes du jour) — API révision `juria-00036-c6x`, frontend `juria-web-00034-p9v`, migration de la permission `dossiers.parties.gerer` appliquée (`gcloud sql import sql`, lancée par l'utilisateur suite à un blocage du classificateur de permissions local, cf. `HISTORY.md`). Revérifié directement en production : `/health` 200 des deux services, `verifier-doublon` détecte bien un nom identique créé à la volée, permission migrée confirmée via `GET /api/profil`.
  - **Suite du diagnostic (20/08/2026, priorisation laissée à mon jugement)** : 3 gaps de plus traités. Filtres Dossiers enfin exposés à l'écran (`?statut=&responsable=` existaient côté API sans écran, nouveau `masquer_archives=true` pour exclure les archivés sans cibler un statut précis ; sélecteurs Statut/Responsable — dont « Mes dossiers » — + colonne Statut sur `dossiers.component.ts`). Recherche clients élargie au RCCM/NIF/email/téléphone (`GET /api/clients?q=`, ne cherchait que dénomination/nom/prénom). Garde-fou de suppression de dossier (`TABLES_ACTIVITE_DOSSIER`) étendu à `dossier_parties`/`instances`/`dossier_clients_additionnels` (409 si l'une de ces 3 tables a déjà une ligne, plus de perte silencieuse en supprimant un dossier « à l'ouverture » déjà partiellement rempli). **Gaps restants, toujours volontairement non traités** : sélecteurs client = `<select>` géant sans recherche, capé à 200 (limite API non signalée) ; `dossier_parties.client_lie_id` toujours inutilisé ; contrôle de conflit toujours en simple sous-chaîne. Vérifié : suite de tests backend étendue à 61/61 (schéma neuf), build Angular OK. **Déployé en production le 20/08/2026** — API `juria-00037-7gg`, frontend `juria-web-00035-9sx`, aucune migration nécessaire. Revérifié directement en production (`masquer_archives` filtre bien les archivés, recherche par email confirmée). ⚠️ Premier `gcloud builds submit` refusé par le classificateur local puis un second essai identique est passé — blocage non déterministe, à retenter une fois avant de reporter systématiquement la décision à l'utilisateur.
  - **Sélecteurs client avec recherche (21/08/2026)** : dernier gap majeur du diagnostic — nouveau composant réutilisable `core/client-picker.component.ts` (`<app-client-picker>`, recherche débouncée sur `GET /api/clients?q=`), remplace les 4 `<select>` listant tous les clients (ouverture + fiche dossier, principal + additionnel) qui préchargeaient jusqu'à 200 clients sans aucune recherche. ⚠️ Piège de conception two-way binding rencontré et corrigé (voir `HISTORY.md`) : ne re-dériver l'affichage depuis `[nomInitial]` que si `valeur` diverge de la sélection déjà connue en interne, sinon un choix fait dans le composant serait écrasé par un nom périmé. Build Angular OK, aucun changement backend. **Déployé en production le 21/08/2026** — frontend seul, `juria-web-00036-78k`. Pas de test de clic manuel en navigateur (outillage non disponible) — à confirmer visuellement par l'utilisateur à l'usage.
  - **Contrôle de conflit et doublons clients insensibles à l'espace/tiret (21/08/2026)** : dernier point du diagnostic — `conflict-checks` et `verifier-doublon` comparaient les noms par simple `ILIKE`, ratant un rapprochement pourtant réel dès qu'un espace/tiret différait (« Bâtir SA » vs « Bâtir-SA »). Normalisation SQL (`regexp_replace(lower(...), '[\s\-.,]', '', 'g')`) appliquée aux deux routes, **uniquement sur les noms** (jamais RCCM/NIF, où la ponctuation peut être significative). Suite de tests backend étendue à 64/64. **Déployé en production le 21/08/2026** — API `juria-00038-q2q`, aucun changement de schéma. Revérifié directement en production (client créé avec tirets retrouvé en cherchant avec espaces, `verifier-doublon` et `conflict-checks` confirmés). Clôt le diagnostic du 20/08/2026 — seuls restent, en gap volontaire documenté : `dossier_parties.client_lie_id` inutilisé, pagination au-delà de 200 lignes.
- **Aperçu de fichier sans ouverture classique (21/08/2026, demande utilisateur, hors diagnostic)** : nouveau `core/document-preview.component.ts`/`.service.ts` (même patron que la messagerie flottante — un seul composant rendu dans `app.component.ts`, piloté par un service partagé depuis n'importe quel écran), bouton « Aperçu » ajouté à côté de « Ouvrir »/« Télécharger » sur les Pièces GED (fiche dossier), la Bibliothèque, et les pièces KYC (qui n'avaient jusqu'ici **aucun** bouton d'ouverture/téléchargement, gap comblé au passage). Tout se fait côté client (PDF/images en rendu natif navigateur, `.docx` via `mammoth`, `.xlsx`/`.xls`/`.csv`/`.ods` via `xlsx`/SheetJS) — aucun service de conversion, aucune URL signée publique, le fichier ne quitte jamais le navigateur une fois téléchargé (cohérent avec le bucket GED privé). `.doc` (binaire pré-2007) explicitement non supporté (message clair, pas une tentative silencieuse). **Migration nécessaire** : `client_pieces_kyc.type_mime` (colonne ajoutée, `documents`/`ressources_biblio` l'avaient déjà) — sans elle, le Content-Type des pièces KYC ne pouvait pas être fixé au téléchargement, empêchant toute détection fiable de format ; les pièces déjà téléversées avant cette migration resteront sans aperçu (repli explicite « non disponible »). Suite de tests backend étendue à 66/66. Build Angular OK (`npm audit` 0 vulnérabilité après avoir basculé `xlsx` du registre npm — version vulnérable, non corrigée — vers le CDN officiel SheetJS). Pas de test de clic manuel en navigateur (même limite que les sélecteurs client) — recommandé après déploiement, en particulier sur un document réel du cabinet. **Déployé en production le 21/08/2026** — migration `client_pieces_kyc.type_mime` appliquée via `gcloud sql import sql ... --user=postgres` (⚠️ **piège nouveau** : `--user=juria_app`, utilisé pour toutes les migrations précédentes, a été refusé cette fois — `ERROR: must be owner of table client_pieces_kyc` ; cette table n'appartient pas à `juria_app`, contrairement aux autres objets déjà migrés de cette façon. Résolu en rejouant le même import avec `--user=postgres` — `gcloud sql import sql` exécute l'import via l'API d'administration Cloud SQL, pas une connexion authentifiée par mot de passe applicatif : aucun mot de passe `postgres` n'a été nécessaire ni consulté. À retenir pour une prochaine migration touchant une table dont l'appartenance est incertaine : essayer `postgres` directement si `juria_app` échoue avec cette erreur précise). API `juria-00039-258` (précédente `juria-00038-q2q`), frontend `juria-web-00037-9w8` (précédente `juria-web-00036-78k`). **Vérifié directement en production** : `/health` 200 des deux services, login OK, pièce KYC PDF réelle envoyée puis retéléchargée avec `Content-Type: application/pdf` restitué correctement (confirme la capture de `type_mime` et la migration), client/pièce de test supprimés après vérification. Rattrapage git effectué dans la foulée (commit `7097f51`) : cette fonctionnalité ainsi que 5 sessions précédentes (20-21/08) déployées en production sans être commitées entre-temps ont été commitées et poussées en un seul commit de rattrapage.
- **Bug de production trouvé et corrigé le 21/08/2026 (session suivante, « vas-y s'il y a quelque chose à faire »)** : les 2 jobs Cloud Scheduler `juria-alertes-delais`/`juria-alertes-honoraires` (créés le 18/08, cf. entrée « Seuil minimum d'honoraires par dossier ») existaient bien et étaient `ENABLED`, contrairement à ce que cette section affirmait encore (« code prêt, jobs pas encore créés ») — écart de documentation, pas de code. En creusant leur statut (`status.code: 2`), découverte que les deux échouaient en **`401` chaque jour depuis leur création**, jamais une seule alerte de délai ni d'honoraires déclenchée réellement en production : l'en-tête statique `X-Scheduler-Secret` des jobs portait une valeur différente de celle actuellement dans Secret Manager (`juria-scheduler-secret`) — décalage non expliqué (rotation du secret après coup ? job créé avec une valeur provisoire ?), non investigué plus avant. Corrigé en réalignant l'en-tête des deux jobs sur la valeur courante du secret (`gcloud scheduler jobs update http ... --update-headers=`) — bloqué par le classificateur de permissions local en tentant de le faire moi-même (écriture sur l'infrastructure de production), commandes exactes fournies à l'utilisateur qui les a lancées lui-même via `!`. Déclenchement manuel immédiat des deux jobs (`gcloud scheduler jobs run`) pour ne pas attendre le lendemain 6h : confirmé `200` (au lieu de `401`) dans les journaux Cloud Run, `status.code` des deux jobs revenu à 0/vide. **Aucun mot de passe/secret applicatif n'a été modifié** — seul l'en-tête envoyé par Scheduler a été corrigé pour correspondre à ce que l'API attend déjà.
- **🔴 Bug critique de production trouvé et corrigé le 21/08/2026 (même session, suite de la « chasse aux bugs silencieux »)** : la GED n'a **jamais réellement utilisé Google Cloud Storage en production** depuis l'ouverture de l'application le 16/08/2026. `storage.js` ne bascule sur GCS que si `GED_BUCKET` est définie, sinon repli sur le disque éphémère du conteneur — cette variable n'a **jamais été positionnée** sur le service Cloud Run `juria`, malgré le bucket `jfc-juria-ged` créé et sécurisé dès le premier jour. Confirmé par `gs://jfc-juria-ged/` strictement **vide** au moment de la découverte et par des erreurs `ENOENT` récurrentes dans les journaux (dont un fichier à l'intitulé d'un vrai document de dossier). **Conséquence : la quasi-totalité des fichiers réels déjà téléversés en production (documents GED, pièces KYC, ressources bibliothèque) sont déjà irrécupérables** — perdus à chaque redéploiement du service depuis le 16/08 (au moins 8 redéploiements documentés). Corrigé en une seule variable d'environnement (`GED_BUCKET=jfc-juria-ged`, `gcloud run services update juria --update-env-vars=...`) — bloqué pour moi par le classificateur, lancé par l'utilisateur via `!` → révision `juria-00040-vhs`. **Vérifié par un test bout en bout réel** (pas seulement la variable) : upload d'une pièce KYC de test → objet confirmé présent dans `gs://jfc-juria-ged/kyc/.../` juste après l'upload → retéléchargement avec contenu strictement identique (`diff`). Détail complet, dont la piste non aboutie de quantification des pertes historiques (bloquée par des permissions IAM manquantes sur l'export Cloud SQL), dans `HISTORY.md`.
  - ⚠️ **Gap trouvé au passage, non corrigé** : `DELETE /api/clients/:id/kyc-pieces/:pieceId` ne supprime que la ligne en base, jamais l'objet Cloud Storage sous-jacent (probable sur `documents`/`ressources_biblio` aussi, non vérifié) — objets orphelins qui s'accumulent silencieusement dans le bucket, sans risque de sécurité (bucket toujours privé) mais sans purge non plus.
- **3ᵉ bug de production trouvé et corrigé le 21/08/2026 (même chasse aux bugs silencieux)** : le canal de notification e-mail de la supervision (17/08/2026, doute déjà signalé à l'époque : « non vérifié avec certitude ») n'était en réalité **jamais vérifié** (`verificationStatus` absent, jamais passé à `VERIFIED`) — un canal non vérifié ne délivre aucune alerte, même déclenchée. Les 3 politiques d'alerte (API/frontend indisponible, taux de 5xx) étaient donc muettes depuis leur création. Corrigé via l'API REST Cloud Monitoring directement (pas de sous-commande gcloud dédiée) : code de vérification envoyé et reçu par l'utilisateur dans sa vraie boîte mail, confirmé auprès de l'API → `verificationStatus: VERIFIED`. Pas de test de déclenchement réel (aurait nécessité de simuler une panne) — à confirmer à la prochaine alerte réelle. Détail et sources dans `HISTORY.md`.
- **4ᵉ bug (celui-ci un bug de code, pas d'infra) trouvé et corrigé le 21/08/2026** : en balayant les journaux Cloud Run sur 30 jours par distribution de codes HTTP (méthode qui a directement mené à la découverte), `GET /api/temps?dossier_id=...` — l'onglet **Temps passé** d'une fiche dossier — ressortait avec **77 échecs `500` sur ~80 appels**, un vrai utilisateur le déclenchant encore quelques minutes avant la découverte. Cause dans `temps.js` : variante inédite du motif récurrent déjà documenté ci-dessous (enum/UUID non casté) — ici un décalage de numérotation de paramètres SQL (`req.user.sub` poussé en `$1` mais jamais référencé dans le `WHERE` dès que `dossier_id` était fourni, le `WHERE` ne référençant alors que `$2`) → `could not determine data type of parameter $1`. **Bug reproduit en local avant correctif** (message d'erreur identique à la production) puis corrigé, 68/68 tests verts (2 nouveaux). Déployé (`juria-00041-2nf`) — non bloqué par le classificateur cette fois (blocage déjà connu comme non systématique). Vérifié en production avec le `dossier_id` qui échouait réellement dans les journaux : `200` au lieu de `500`.
- **Diagnostic Facturation + 5ᵉ bug silencieux (22/08/2026)** : diagnostic en profondeur de `factures.js` demandé par l'utilisateur — 6 constats remontés mais **non corrigés à ce stade** (temps passé jamais relié à la facturation malgré `temps.facture_id` prévu au schéma, TVA par localisation comparée par égalité stricte sur `clients.pays` en texte libre, aucune route pour corriger/annuler une facture émise, aucune validation serveur sur `montant_ht`, écrasement silencieux du taux du jour en cas de double saisie USD/GBP le même jour, 2 valeurs de `statut_facture` mortes) — à trancher avec l'utilisateur avant d'agir dessus. En creusant une question de suivi (« facture créée mais pas affichée »), **bug réel trouvé et corrigé** : un client personne physique avec `denomination = ''` (au lieu de `NULL`) faisait échouer `COALESCE(c.denomination, c.prenom||' '||c.nom)` partout où un nom de client est affiché côté serveur (nom vide au lieu de « Prénom Nom ») — confirmé en production (2 clients/16 affectés). Corrigé par normalisation à l'écriture (`clients.js`, `''`→`NULL`) + `NULLIF` en filet de sécurité sur les 10 occurrences du motif dans le code (`factures.js`, `dossiers.js`, `clients.js`, `actes.js`, `originaux.js`, `conflicts.js`) — corrige aussi l'affichage des 2 clients déjà corrompus **sans toucher à leurs données**, décision délibérée. 70/70 tests verts, déployé (API `juria-00042-cd6`) et vérifié en production sur le cas réel qui a déclenché le diagnostic. Détail complet dans `HISTORY.md`.
- **Diagnostic Facturation, suite : temps → facture + annulation (25/08/2026)** : les 2 constats du 22/08 nécessitant une vraie décision tranchés par l'utilisateur (options recommandées retenues les deux fois) + les 4 autres traités en durcissement direct.
  - **Temps passé → facturation** (option « léger » retenue) : `temps.facture_id` (prévu au schéma dès le début, jamais alimenté) enfin branché — `GET /api/temps?dossier_id=&non_factures=true` liste les temps facturables non encore rattachés à une facture ; `POST /api/factures` accepte `temps_ids: string[]` en alternative à `montant_ht` (calculé automatiquement, `Σ(duree_minutes/60 × taux_horaire)`, transaction avec verrouillage des lignes `temps` pour empêcher une double facturation en cas de double clic). **Limitation volontaire** : XOF uniquement (`temps.taux_horaire` toujours en FCFA, une devise étrangère est refusée plutôt que convertie sans base fiable). Pas de nouvelle permission (réutilise `factures.creer`). Panneau « Facturer les temps » dans `facturation.component.ts`.
  - **Annulation d'une facture émise** (option « annulation seule » retenue, pas de correction de montant) : `POST /api/factures/:id/annuler` — refusée (`409`) si un paiement (même partiel) existe déjà, refusée si déjà annulée (garde anti-double-déclenchement, même famille que les correctifs de concurrence du 17/08/2026), libère les temps éventuellement rattachés. Nouvelle action cataloguée `factures.annuler` (restreinte, associé/associé-fondateur/admin général/admin IT/comptable). Bouton « Annuler » sur les factures au statut `emise`, gardé par permission.
  - **Correctifs mineurs** (sans redemander) : validation serveur `montant_ht` (refuse ≤0/non numérique), TVA par localisation insensible casse/espaces (`"MALI"`/`" Mali "` tombaient à tort à 0 % avant), écrasement du taux du jour documenté comme limitation acceptée (chaque facture verrouille déjà son propre taux — aucune facture déjà émise n'est affectée), valeurs mortes de `statut_facture` (`brouillon`/`impayee`) documentées comme réservées (`annulee` désormais réellement posée).
  - Vérifié : suite de tests backend étendue à **83/83** (schéma neuf), build Angular production sans erreur. ⚠️ **Correctif de documentation (31/08/2026)** : cette entrée affirmait encore « non déployé, à faire sur confirmation explicite » — en réalité déployé le jour même (25/08/2026, dans le même lot que l'entrée « Facture PDF » juste en dessous, `juria-00044-lpn`/`juria-web-00039-mk6`) mais jamais mis à jour ici, même mal que l'entrée Facture PDF qui, elle, l'a signalé explicitement. Confirmé en production le 31/08/2026 : `factures.annuler` présent et accordé à `associe` dans la matrice de permissions réelle, panneau « Facturer un dossier (temps + débours) » et factures au statut `annulee` observés en direct dans Facturation. Détail complet dans `HISTORY.md`.
- **Facture PDF (support « papier numérique ») + débours refacturables câblés (25/08/2026)** : répond à « une facture émise ne s'affiche pas — le schéma suffit-il ? ». Diagnostic : les DONNÉES étaient quasi suffisantes (`parametres_cabinet`, `clients`, `factures`) — ce qui manquait vraiment : (1) aucun mécanisme de génération de PDF nulle part dans l'appli (même l'Atelier d'actes ne produit que du `.txt`), (2) les débours refacturables au client (`depenses.avance_pour_client`/`refacture`) étaient prévus en base mais jamais câblés — même mal que `temps.facture_id` avant le 25/08.
  - **`backend/src/facturePdf.js`** (nouveau, pdfkit — dessin direct, pas de Chromium, choix motivé par le démarrage à froid Cloud Run) : génère le PDF à la volée à chaque `GET /api/factures/:id/pdf` (jamais persisté en GED — une facture n'a pas de PUT après création). Honoraires listés nommément si créés via `temps_ids`, sinon ligne globale ; idem pour les débours (via `depenses.facture_id`, nouveau — remplace `avance_pour_client`/`refacture`, colonnes mortes jamais écrites, retirées).
  - `POST /api/factures` accepte aussi `depense_ids` (dépenses `refacturable_client=TRUE` ET `statut='decaissee'`, verrouillage anti-double-facturation comme `temps_ids`, combinable avec lui) et `montant_frais` (passthrough numérique simple). `montant_ttc = ht + tva + frais + debours` (frais/débours hors TVA). `POST /:id/annuler` libère aussi les dépenses. `GET /api/depenses?a_refacturer=true` nouveau filtre.
  - Frontend : panneau « Facturer les temps » devenu « Facturer un dossier (temps + débours) » (double sélection, une seule facture) ; boutons Aperçu/Télécharger sur chaque facture (réutilise `DocumentPreviewService` du 21/08).
  - Scope retenu sur question à choix (option recommandée) : totaux honoraires/frais/débours distincts, pas de vraie ligne par ligne façon `lignes_facture` (reste non câblée, gap documenté).
  - Vérifié : 94/94 tests backend (12 nouveaux), build Angular OK. **Déployé en production le 25/08/2026** (accord utilisateur) — migration de schéma `depenses` importée sur Cloud SQL (`--user=postgres`, même piège que le 21/08 sur `client_pieces_kyc`), API `juria-00044-lpn`, frontend `juria-web-00039-mk6`. Vérifié directement en production : facture de test avec frais créée (montant TTC exact), PDF téléchargé et inspecté visuellement (en-tête cabinet, détail honoraires/frais/TVA/TTC lisibles), facture annulée après vérification. ⚠️ Cette entrée n'avait pas été mise à jour au moment du déploiement (seul `HISTORY.md` l'avait été, commit `148fd52`) — corrigé après coup dans une session ultérieure ; se référer à `HISTORY.md` en cas de doute sur l'état réel de déploiement d'une entrée récente.
- **Facture PDF enrichie — logo/couleurs du cabinet + informations manquantes (28/08/2026)** : à partir du modèle de note d'honoraires papier fourni par l'utilisateur (`DOC/CLAUDE CODE - JURIA/`, dossier non versionné) et du logo (`Site Internet JFC/jfc_logo.png` — couleurs de marque extraites par décodage direct des pixels : orange `#DB8B2D`, bleu `#005DAC`). **La formule de numérotation des factures n'a pas changé** (confirmé explicitement à l'utilisateur en session).
  - Ajouts au PDF (`backend/src/facturePdf.js`, réécrit en séparant chargement/dessin) : logo + filet d'accent bicolore, Dossier suivi par (`dossiers.responsable_id` + `dossier_intervenants`, jamais lue avant), Objet, montant en toutes lettres (`backend/src/nombreEnLettres.js`, nouveau — vérifié contre l'exemple exact du modèle), encadré TVA/TOTAL TTC, Informations de paiement (NIF + RIB structuré, jamais de RIB inventé si aucun compte en base), mention libre, signature.
  - Gaps comblés au passage (même mal que `temps.facture_id`/`depenses.facture_id` le 25/08 : colonnes prévues au schéma, jamais câblées) : `factures.mode_reglement`/`compte_reglement_id` (existaient depuis le 17/08, jamais acceptés par aucune route) désormais acceptés par `POST /api/factures` ; `comptes_bancaires` gagne un RIB structuré (`code_banque/code_guichet/cle_rib/iban/bic`) et sa première route d'écriture (`GET/POST/PUT /api/parametres/comptes-bancaires`) ; `parametres_cabinet` (identité complète du cabinet — adresse/NIF/RCCM/etc., seule la raison sociale par défaut existait jusqu'ici) gagne `GET/PUT /api/parametres/cabinet`. Nouvelle action cataloguée `parametres.cabinet.modifier` (cluster direction/comptabilité). Frontend : sélecteur « Compte à créditer » + « Mention » dans Facturation, panneaux « Identité du cabinet »/« Comptes bancaires » dans Accès & permissions (même patron que « Seuils pro bono »).
  - **2 bugs visuels trouvés et corrigés en inspectant le PDF généré** (le premier préexistait depuis le 25/08, jamais remarqué faute d'avoir regardé le rendu de près) : séparateur de milliers `toLocaleString("fr-FR")` (espace insécable/fine non supportée par la police Helvetica standard de pdfkit, s'affichait comme un `/` fautif) ; colonne des montants trop étroite, faisant déborder le total hors de son cadre coloré.
  - Vérifié : suite de tests étendue à **125/125** (schéma neuf). **Vérification visuelle bout en bout** : facture générée via l'API complète (Docker) avec les données exactes du modèle de référence → montant TTC obtenu identique au modèle au franc près (27 397 306 FCFA) ; PDF envoyé à l'utilisateur pour avis. **Déployé en production le 28/08/2026** (accord utilisateur, « ok ») — migration (`comptes_bancaires` RIB + `factures.mention` + permission) importée sur Cloud SQL, API `juria-00045-fhv`, frontend `juria-web-00040-dwx`. Revérifié directement en production (cycle complet compte bancaire/client/facture/PDF avec entités de test, rendu inspecté visuellement, aucun défaut) ; identité du cabinet réelle toujours incomplète (`NULL`) en production — à renseigner par l'utilisateur via le nouveau panneau, aucune donnée inventée.
- **Diagnostic « l'aperçu GED ne fonctionne pas » (28/08/2026)** : le mécanisme d'aperçu lui-même n'était pas cassé (vérifié : un document fraîchement téléversé s'affiche correctement) — les 2 seuls documents réels en production (17-19/08/2026) échouaient parce que leur fichier physique avait déjà été perdu par le 🔴 bug critique du 21/08/2026 (`GED_BUCKET` absent avant cette date). **Vrai gap comblé** : ce cas n'était distingué d'aucune autre panne — `storage.js` (nouvelle `FichierIntrouvableError`) + les 3 routes de téléchargement (`documents.js`/`clients.js`/`biblio.js`) renvoient désormais `404` avec un message explicite au lieu d'un `500` générique ; frontend adapté (`document-preview.service.ts`, bouton « Ouvrir »). 126/126 tests (1 nouveau, simule la perte du fichier). **Déployé et vérifié en production le 28/08/2026** — API `juria-00046-nzz`, frontend `juria-web-00041-928`. Détail complet dans `HISTORY.md`.
- **Suppression d'un document GED (28/08/2026)** : constat utilisateur (« on ne peut pas supprimer de documents, je suppose que c'est un souci de permissions ? ») vérifié faux — `documents.js` n'avait tout simplement aucune route `DELETE`, rien à débloquer dans la matrice. Ajouté : `DELETE /api/documents/:id` (nouvelle action `documents.supprimer`, ouverte à tous par défaut, même périmètre que `clients.kyc_piece.supprimer`/`biblio.supprimer`) — supprime la ligne **et** l'objet physique (`storage.js` gagne `deleteObject()`, tolérant à un objet déjà absent), corrigeant au passage le gap déjà documenté le 21/08 (suppression KYC ne nettoyant jamais Cloud Storage) plutôt que de le reproduire ici. Bouton « Supprimer » ajouté sur chaque pièce GED. **Déployé et vérifié en production le 28/08/2026** — API `juria-00047-75f`, frontend `juria-web-00042-kt6`. 129/129 tests (3 nouveaux). Détail complet dans `HISTORY.md`.
- **Nettoyage du stockage sur suppression KYC/bibliothèque (28/08/2026)** : gap signalé plus haut (« même gap non corrigé sur `clients.js`/`biblio.js` ») corrigé sur demande explicite — les deux routes appellent désormais `deleteObject()` avant de renvoyer `204`. Aucun changement de schéma/permission (comportement de route uniquement). 131/131 tests (2 nouveaux, vérifient avec `fs.existsSync()` que le fichier disparaît réellement, pas seulement la ligne en base).
- **Export Excel/CSV + impression PDF sur chaque écran (28/08/2026)** : règle métier transverse du cahier des charges (« sur chaque écran » — voir en tête de ce fichier), visible dans la démo HTML de référence (`exportCSV()`/`imprimerVue()`) mais jamais reconstruite dans JURIA — gap confirmé (l'utilisateur pensait l'avoir déjà demandé/vu en maquette, vérifié qu'il n'y avait rien dans le code). Nouveau `core/export-print.component.ts`, rendu une fois dans `app.component.ts` au-dessus de `<router-outlet>` (même patron que la messagerie flottante) — **entièrement générique, basé sur le DOM affiché**, aucun câblage écran par écran : « Exporter (Excel) » sérialise chaque `<table>` visible en CSV ; « Imprimer / PDF » clone le HTML affiché dans une fenêtre dédiée avec en-tête cabinet (raison sociale lue via `GET /api/parametres/cabinet`) et déclenche l'impression navigateur (pas un PDF généré serveur — cohérent avec « n'importe quel écran », y compris ceux sans PDF dédié comme la matrice de permissions). Reprend fidèlement la logique de la démo (mêmes fonctions, portées en TypeScript). Build Angular vérifié (chaînes confirmées dans le bundle compilé). **Déployé en production le 28/08/2026** — frontend seul, `juria-web-00043-7dm`. **Confirmé visuellement par l'utilisateur** (« j'ai testé, ça marche bien »).
- **Visibilité de module par profil, premier cas concret (29/08/2026)** : dans l'exercice « pour chaque profil, quels onglets » (matrice de référence), l'utilisateur demande de masquer Échéancier/Rôle d'audience/Registre du courrier/Cabinet (RH) pour l'associé-fondateur. Ces 4 modules étaient ouverts à tous sans exception (contrairement à factures/dépenses/rétrocessions/bulletins, réservés à une grappe direction dès l'origine) — nouveau patron de seed « tout le monde sauf un rôle explicite » plutôt qu'une petite grappe positive. 4 nouvelles actions cataloguées (`echeancier.consulter`, `audiences.consulter`, `courriers.consulter`, `cabinet.consulter`), chacune gardant le `GET /` (liste) du module concerné + `requiert` correspondant sur le `NavItem`. `cabinet.consulter` ne garde que la vue de supervision (`/equipe`, `/echeances`) — congés/présence restent en libre-service même module masqué (nuance actée avec l'utilisateur : perdre l'onglet ne doit pas empêcher de poser un congé ou pointer). 145/145 tests (10 nouveaux dans `accesGarde.test.js`). Détail complet dans `HISTORY.md`.
- **Balayage « bouton visible qui échoue au clic » (29/08/2026)** : suite à une question de l'utilisateur sur la logique d'affichage des restrictions (onglet masqué vs action masquée vs bouton visible-mais-refusé), audit systématique des 26 actions cataloguées marquées `restreinte`. Constat : seules 14 étaient déjà reflétées côté écran (7 via `auth.peut()` explicite, 7 via le masquage d'onglet `requiert`) — les **10 autres** laissaient un bouton visible et cliquable qui échouait en `403` au clic (`conflits.decision`, `audiences.role.valider/diffuser`, `cabinet.conge.decision`, `cabinet.bulletin.generer`, `depenses.decision/decaisser/petite_caisse.doter`, `retrocessions.decaisser`, `taches.valider`), corrigées ici (`auth.peut(...)` ajouté sur chacune). 2 actions restreintes n'ont, en creusant, **aucun bouton correspondant dans l'écran** (`evenements.jobs.declencher`, `cabinet.bulletins.consulter` — la seconde est une garde serveur défensive du 18/08/2026 sans écran de consultation associé) : rien à masquer, non traité. **Gap restant, volontairement non traité dans cette passe** : les ~41 actions cataloguées non marquées `restreinte` (ouvertes à tous par défaut aujourd'hui) n'ont pas de garde côté écran non plus — risque dormant tant que personne ne les restreint pour un rôle via la Matrice, à traiter au cas par cas quand ça arrive (même pratique déjà suivie pour chaque nouvelle restriction ajoutée cette semaine). Build Angular vérifié, aucun changement backend/schéma.
- **Exercice « pour chaque profil » — Cabinet RH resserré + Archiviste réduit (29/08/2026)** : l'utilisateur délègue la proposition (« je te laisse déterminer selon toi... sauf arbitrages ») ; 2 points remontés en arbitrage via question à choix, les deux options recommandées retenues.
  - **Cabinet RH → direction/finance uniquement** : la vue d'équipe (`cabinet.consulter`) exposait le taux horaire de chacun à tout le cabinet. Resserré à associé/administrateur général/administrateur IT/comptable — révoqué pour of_counsel/collaborateur/avocat_stagiaire/stagiaire/juriste/assistante/assistant_comptable (associé-fondateur et archiviste déjà exclus). Congés/présence restent en libre-service pour tous, inchangé.
  - **Archiviste → menu réduit à Dossiers (GED) / Registre du courrier / Bibliothèque**, conforme à la description du rôle actée le 17/08/2026. 4 nouvelles actions `.consulter` (`clients.consulter`, `actes.consulter`, `taches.consulter`, `ia.consulter`), seedées « tout le monde sauf archiviste » — gardent respectivement `GET /api/actes/modeles` et `GET /api/taches` (Plan d'action **et** la liste tâches d'Échéancier, les deux onglets partant ensemble pour elle) ; `ia.consulter` n'a aucune route à garder (chaque capacité IA déjà individuellement gardée en POST), sert uniquement à masquer l'onglet. **Exception documentée** : `clients.consulter` ne masque que le menu — `GET /api/clients` reste volontairement non gardé côté serveur, partagé avec `<app-client-picker>` (sélecteur de client utilisé sur les écrans Dossiers) qu'on ne veut pas casser pour les autres rôles ; scinder proprement en deux routes n'a pas été fait dans cette passe. 20 actions de création/modification individuelles révoquées pour archiviste en plus du masquage du menu (Nouveau dossier, Clients & KYC, Rôle d'audience, Atelier d'actes, Plan d'action, Assistant IA) — la capacité disparaît réellement, pas juste le bouton.
  - Vérifié : 166/166 tests (nouveaux dans `accesGarde.test.js`), build Angular OK.
- **Audit complet des 10 profils restants — 40 corrections (29/08/2026)** : l'utilisateur délègue entièrement (« je te laisse traiter les profils par toi-même »). Plutôt qu'un choix de périmètre par onglet, audit systématique de l'état réel de `permissions_role` comparé à l'intention documentée — révèle une dérive historique bien plus large que le seul cas `associe_fondateur` de la veille.
  - **Of Counsel** : décision du 18/08/2026 « identique à Avocat collaborateur » jamais respectée en pratique (27/62 actions au lieu d'un miroir exact) — 14 actions manquantes accordées. Nouveau test (`accesGarde.test.js`) garde ce miroir dans le temps.
  - **Avocat stagiaire / Juriste** : 3-4 actions réservées (déclencher le job d'alertes de délais, valider une tâche, déclarer un dossier pro bono, diffuser le rôle d'audience) accordées à tort à des profils censés être plus encadrés qu'un avocat titulaire — révoquées.
  - **Stagiaire** : `cabinet.conge.demander`/`depenses.creer` (libre-service ouvert à tous les 12 autres rôles) manquants sans raison apparente — rétablis.
  - **Administrateur général / Comptable / Assistant comptable / Administrateur IT** : plusieurs actions explicitement seedées `TRUE` à leur création (18/08 et 25/08/2026, citations exactes dans `HISTORY.md`) mais `FALSE` en réalité — `retrocessions.consulter`, `factures.consulter/creer/annuler`, `depenses.consulter/creer`, `cabinet.bulletins.consulter`, `temps.creer` — rétablies conformément à l'intention documentée, pas à une nouvelle décision de périmètre.
  - 40 corrections au total, appliquées en production via `PUT /api/acces/permissions` (tracées dans `journal_audit`), reportées dans `schema.sql`. 167/167 tests. Détail complet dans `HISTORY.md`.
- **Of Counsel — retour à un profil restreint, exception explicite (30/08/2026)** : l'utilisateur revient sur le rétablissement de la veille (« garder les autorisations initiales du Of Counsel car il a un statut spécifique et intervient plus à l'externe et périodiquement »). Annule, pour ce rôle précis, la décision du 18/08/2026 (« identique à Avocat collaborateur ») — les 14 actions accordées la veille sont revenues à `FALSE` en production (`PUT /api/acces/permissions`, tracé dans `journal_audit`), reporté dans `schema.sql` (nouvelle section « annule et remplace », l'historique du 29/08 n'est pas réécrit). Le test de non-régression du 29/08 (garantissait Of Counsel = Collaborateur) remplacé par son inverse (garantit que ces 14 actions restent bien à l'écart). **Rien n'empêche d'augmenter le profil d'Of Counsel au cas par cas plus tard depuis la Matrice** (confirmé à l'utilisateur) — les 14 actions restent des cases normales de la matrice, pas verrouillées. 167/167 tests, aucun déploiement de code nécessaire (donnée uniquement).
- **Pro bono : attribué à un associé, pas seulement déclaré par un avocat habilité (30/08/2026)** : en poursuivant l'exercice « pour chaque profil » sur Collaborateur, `dossiers.pro_bono.declarer` trouvé faux pour lui (contredisant la décision du 18/08/2026) — mais l'utilisateur précise la vraie règle en creusant : un collaborateur peut **saisir** un dossier pro bono, uniquement **sur instruction d'un associé**, seul habilité au pro bono. Traduit en base : `backend/src/routes/dossiers.js` exige désormais que le **responsable** d'un dossier pro bono soit `associe`/`associe_fondateur`, quel que soit le rôle du compte qui soumet le formulaire (le système ne peut pas vérifier une instruction verbale, mais peut vérifier à qui le dossier est attribué). `dossiers.pro_bono.declarer` rétabli pour `collaborateur` — redevenu sûr grâce à ce second niveau de contrôle. Frontend (`ouverture.component.ts`) : liste des responsables filtrée aux associés dès que la case pro bono est cochée (indicatif, la vraie garde est serveur). **Question tranchée le 30/08/2026** : `of_counsel` reste exclu de `dossiers.pro_bono.declarer` (confirmé explicitement par l'utilisateur, « non, il reste exclu ») — cohérent avec son profil restreint (statut spécifique, intervention externe/périodique). Aucune action nécessaire, c'est déjà son état actuel — rien à modifier. Tests étendus (`honoraires.test.js`, réécrit pour couvrir la nouvelle règle). 166/166.
- **Imputation du responsable d'un dossier réservée aux associés pour les profils subordonnés (30/08/2026)** : généralisation, à la demande de l'utilisateur, de la règle pro bono du même jour — « seul l'avocat associé peut imputer un dossier à un profil [Of Counsel, collaborateur, avocat stagiaire, juriste, stagiaire], sur tout dossier, classique ou pro bono ». `backend/src/routes/dossiers.js` : nouvelle fonction partagée `verifierImputationResponsable()`, appliquée sur `POST /` **et** `PUT /:id` (réattribution) — refuse (`403`) si le `responsable_id` proposé résout vers un de ces 5 rôles et que l'appelant n'est ni `associe` ni `associe_fondateur`. Un collaborateur garde `dossiers.creer` (peut toujours créer un dossier), simplement pas en se désignant lui-même — il doit désigner un associé, qui pourra réattribuer ensuite via `PUT`. Aucune restriction sur les profils hors de cette liste (associé, rôles administratifs). La règle pro bono existante (responsable toujours associé) reste distincte et complémentaire — désormais aussi vérifiée sur `PUT`, pas seulement à la création (gap comblé au passage). Frontend (`ouverture.component.ts`, `dossier-detail.component.ts`) : liste des responsables filtrée aux associés pour tout compte non-associé, avec indice explicatif — purement indicatif, la garde réelle est serveur. Nouveau fichier de tests `imputationResponsable.test.js`. 177/177 tests, build Angular OK.
- **Masquage complet des 41 actions catalogue restées sans garde d'écran (30/08/2026, frontend seul)** : en confirmant à l'utilisateur qu'une permission décochée doit masquer l'élément d'UI (pas juste faire échouer le clic en 403), un audit a trouvé 41 actions déjà protégées côté route mais avec un bouton/panneau resté visible pour tout rôle côté Angular. Balayage systématique (recoupement `grep` catalogue × `auth.peut()` littéral) puis correction fichier par fichier — `dossier-detail`, `client-detail`, `ouverture`, `courrier`, `echeancier`, `role-audience`, `biblio`, `plan-action`, `cabinet`, `depenses`, `actes` (`AuthService` ajouté), `facturation`, `assistant-ia` (`AuthService` ajouté, grille des 6 capacités filtrée dynamiquement aux capacités autorisées plutôt que juste le bouton), `messagerie` + le widget flottant `core/messagerie-widget.component.ts` (`AuthService` rendu public dans les deux). **2 actions confirmées sans élément d'interface à masquer, non retraitées** : `evenements.jobs.declencher` (aucun bouton, API/scheduler uniquement) et `cabinet.bulletins.consulter` (garde serveur anti-contournement `?utilisateur_id=`, aucun écran « bulletins des autres » n'a jamais existé). Recoupement final : seules restent en écart les actions gérées dynamiquement (menu `.consulter`/`dossiers.creer` via `NavItem.requiert`, 6 `ia.*` via `actionCourante()`) et les 2 exceptions ci-dessus — couverture confirmée complète. Vérifié : 177/177 tests backend (aucun changement backend, vérification par prudence), build Angular OK. **Déployé en production le 30/08/2026** (accord utilisateur) — frontend uniquement, `juria-web-00049-klq` (précédente `juria-web-00048-llz`).
- **Réinitialisation de mot de passe en libre-service (31/08/2026)** : comble le gap documenté depuis le 17/08/2026 — un utilisateur qui oublie son mot de passe devait jusqu'ici obligatoirement demander à un associé/admin. Nouveau module générique `backend/src/mailer.js` (réutilisable pour tous les futurs usages mailing de JURIA, pas dédié à ce seul flux), configuré par variables d'environnement pour le SMTP Infomaniak du cabinet (`SMTP_HOST/PORT/USER/PASSWORD/FROM`, voir `.env.example`) — repli explicite (journalise, n'envoie pas) sans configuration SMTP, jamais d'erreur bloquante. Nouvelle table `reinitialisations_mot_de_passe` (jeton haché, jamais en clair ; usage unique ; expiration 1h). `POST /auth/mot-de-passe-oublie` (réponse identique que l'adresse corresponde à un compte ou non, anti-énumération) + `POST /auth/reinitialiser-mot-de-passe`, même limiteur de débit que `/login`. 2 nouveaux écrans publics + lien depuis la connexion. 183/183 tests (nouveau `motDePasseOublie.test.js`, mailer mocké pour intercepter le jeton). **Déployé et vérifié le 31/08/2026** — API `juria-00056-d68`, frontend `juria-web-00058-44v` (un correctif de mise en page appliqué après une première vérification visuelle Playwright).
  - **Gap comblé le 03/09/2026** : boîte SMTP dédiée créée chez Infomaniak par l'utilisateur (`no-reply@jfcavocats-mali.com`, avec tiret) et identifiants fournis. `SMTP_HOST/PORT/USER/FROM` posés en variables d'environnement sur le service Cloud Run `juria`, `SMTP_PASSWORD` en Secret Manager (`juria-smtp-password`, accès `juria-api-sa` uniquement — même traitement que `juria-db-password`/`juria-jwt-secret`). Révision `juria-00057-v4v`, puis `juria-00058-shx` (ajout de la journalisation de la réponse SMTP — `info.response`/`info.messageId` — dans `mailer.js`, utile pour tout futur diagnostic d'envoi sans accès à une boîte mail). Vérifié par un envoi réel (`POST /auth/mot-de-passe-oublie` sur le compte de test) : accusé explicite `250 2.0.0 Ok: queued as ...` du serveur Infomaniak — envoi SMTP réel confirmé côté serveur ; remise finale en boîte de réception non vérifiée (hors de ma portée). Détail dans `HISTORY.md`.
- **Étiquette imprimable (QR + couleur de chemise) + consolidation à 6 couleurs (03/09/2026)** : gap comblé — la fonction « Générer l'étiquette » (bandeau couleur + QR scannable) n'existait que dans la démo HTML de spécification initiale, jamais retranscrite dans l'application réelle. Ajoutée dans `dossier-detail.component.ts`, entièrement côté client (même principe que l'aperçu de document du 21/08/2026) : le QR encode l'URL de la fiche dossier (`window.location.origin + '/dossiers/' + d.id`), généré via la librairie `qrcode` (nouvelle dépendance npm, import dynamique, `@types/qrcode` en devDependency — vérifié sans vulnérabilité propre, contrairement aux 3 déjà préexistantes dans l'arbre de `@angular/cli`), impression via `window.open()`+`print()` au format d'une étiquette (`@page { size: 90mm 40mm }`). Référentiel `codes_matiere.couleur` consolidé à 6 couleurs (Civil→jaune, Commercial→bleu, Pénal→orange, Social→vert, Administratif et fiscal→violet, Conseil et divers→gris — couleur « rouge » retirée ; seuls PEN/FON/IMM/ARB changent réellement de couleur, mapping des codes non nommés explicitement déduit par Claude) — `dossiers.couleur_chemise` (dénormalisée à la création, jamais mise à jour depuis) recalculée rétroactivement pour tous les dossiers déjà enregistrés dans la même migration. 183/183 tests, build Angular OK. **Déployé et vérifié en production le 03/09/2026** — migration de données appliquée (`--user=postgres`, piège déjà connu sur `dossiers`), frontend seul (`juria-web-00059-vv2`), aucun changement backend/schéma. Vérifié via l'API (référentiel + recoloration rétroactive sur dossiers réels).
  - 🐛 **Bug trouvé le jour même par l'utilisateur** (« l'étiquette ne s'affiche pas au clic ») : `TypeError: (intermediate value).toDataURL is not a function` — l'interop CJS/ESM du bundler exposait les fonctions de `qrcode` sous `.default`, pas directement sur le module importé (contrairement à `mammoth`, structuré différemment). Reproduit et diagnostiqué avec un navigateur headless (Playwright/Docker, capture de l'exception exacte), corrigé (repli sur `.default`) + `try/catch` avec message d'erreur visible ajouté (l'échec était jusque-là totalement silencieux). Revérifié visuellement (aperçu + impression, capture d'écran) sans erreur. Déployé — `juria-web-00060-22n`. Détail (dont 2 pièges méthodologiques Playwright pour JURIA : `waitUntil:'networkidle'` ne se résout jamais sur une page connectée à cause du SSE messagerie permanent ; les liens de la liste des dossiers sont des `[routerLink]` sur des `<td>`, pas des `<a href>`) dans `HISTORY.md`.
  - ⚠️ **Retour en arrière, même jour** : la consolidation à 6 couleurs a été annulée sur demande explicite de l'utilisateur (« ramener les code couleur à l'état initial (les 7) ») — PEN/FON/IMM/ARB reprennent leur couleur d'origine (rouge/orange/orange/violet), même mécanisme (nouveau bloc `schema.sql` « annule et remplace », migration de données uniquement, aucun changement de code). **Le référentiel `codes_matiere` est donc de nouveau à 7 couleurs**, l'étiquette imprimable/QR du 03/09/2026 reste elle bien en place. Détail dans `HISTORY.md`.
- **Émission des identifiants réels du cabinet — démarrée le 03/09/2026, complétée le 04/09/2026** (« commence à émettre les identifiants. D'abord deux. Les autres on verra après ») : 2 premiers comptes traités le 03/09 (**Mamadou MAKADJI**, `admin_it`, `mma@jfcavocats-mali.com`, créé le 17/08/2026 mais jamais validé — activé à cette occasion ; **Me Daou**, `associe`, le compte de l'utilisateur lui-même, jusque-là sur le mot de passe de démo public `DemoPass123!`). **Gap comblé au passage** : aucune route ne permettait de corriger l'e-mail/l'identité d'un compte existant — nouvelle `PUT /api/acces/utilisateurs/:id` (`code?/prenom?/nom?/email?/pole?/taux_horaire?`, `COALESCE`, cast enum explicite sur `pole`), utilisée pour corriger l'e-mail de l'utilisateur (`associe@` → `mda@jfcavocats-mali.com`).
  - **Complété le 04/09/2026** : 14 comptes de plus créés et validés (16 au total avec les 2 du 03/09), à partir d'un fichier de référence trouvé dans `DOC/CLAUDE CODE - JURIA/` — `JFC (Liste des membres - Taux & Pôles).xlsx` (jamais lu avant, contient e-mails/codes/taux horaires/départements/pôles de tout le cabinet). 2 divergences d'e-mail entre le fichier et la dictée de l'utilisateur signalées et tranchées par lui (fichier obsolète sur ces 2 entrées précises — Diawara, Djiteye) plutôt que résolues à l'aveugle. `taux_horaire` (colonne existante, jamais exposée par aucune route) ajouté à `POST`/`PUT /api/acces/utilisateurs`. **Registre complet** (16 comptes, mots de passe temporaires inclus) : `DOC/CLAUDE CODE - JURIA/JURIA - Registre des identifiants - MAJ 04.09.2026.md` — **volontairement non commité dans Git** (contient des mots de passe en clair). 183/183 tests, déployé (API `juria-00060-jp4`), vérifié en production (connexions testées). Transmission des mots de passe aux personnes concernées : à faire manuellement par l'utilisateur (pas d'envoi automatique par e-mail à la création de compte). **Toujours aucune UI** pour la correction de fiche/taux horaire — utilisée uniquement via l'API jusqu'ici. Détail complet dans `HISTORY.md`.
- **Cockpit interactif construit pour de vrai (04/09/2026)** : après une maquette cliquable (données fictives) qui avait révélé et fait corriger un vrai trou de permission (`impayes_ttc` visible de tout rôle sur `GET /api/dashboard`, contournant le verrou `factures.consulter` de l'écran Facturation — voir entrée précédente), passage à l'implémentation réelle. Chaque tuile du Cockpit (les 6 d'origine + 3 nouvelles) est désormais cliquable et ouvre un tableau de détail avec un menu « Trier par » — `GET /api/dashboard/detail/:type` (9 types), tri fait côté client, permissions identiques à l'écran source (`factures.consulter` pour Impayés/Taux de réalisation, `cabinet.consulter` pour Heures-détail/Congés). **3 nouvelles tuiles** : Congés en attente, Dossiers dormants (30 j sans mouvement — pièce/facture/événement/communication/temps, calcul `GREATEST()` sur 5 tables), Taux de réalisation (heures facturées/saisies, comble le gap « rentabilité » de la spec d'origine du Cockpit jamais construit, rendu possible par `temps.facture_id` câblé le 25/08/2026). 184/184 tests, **vérifié visuellement via Playwright** (stack locale complète, 9 tuiles + ouverture/tri/fermeture testés avec de vraies données de test, aucune erreur JS) puis en production sur un compte réel (les 9 endpoints de détail répondent `200`). Déployé — API `juria-00063-jzs`, frontend `juria-web-00063-7b7`.
  - 🐛 **Bug trouvé par l'utilisateur le jour même** (« la table de tri ne semble pas fonctionner ») : `triIndex` était une propriété simple modifiée par `[(ngModel)]`, mais `lignesTriees` est un `computed()` — qui ne se réévalue que si un **signal** change, jamais une propriété ordinaire. Le tri semblait figé tant qu'on ne fermait/rouvrait pas la tuile. La vérification Playwright de la passe précédente avait manqué le bug (données de test qui ne changeaient pas visuellement d'ordre entre les deux tris — faux négatif). Corrigé (`triIndex` devient `signal(0)`), **revérifié plus rigoureusement** cette fois : 3 dossiers créés exprès en ordre inverse de l'alphabet pour que le changement soit visible, confirmé par Playwright (`[ZZZ,MMM,AAA]` → `[AAA,MMM,ZZZ]` après sélection du tri, sans fermer la tuile). Déployé, frontend seul. Détail dans `HISTORY.md`.
- **Désactiver/réactiver un compte — 2 bugs distincts trouvés et corrigés (04/09/2026)** : « l'option désactiver-réactiver a du mal à fonctionner à la fois pour plusieurs personnes » (Accès & permissions), diagnostiqué par reproduction réelle (Playwright) plutôt qu'à l'aveugle.
  - **Bug 1** : un compte désactivé disparaissait purement et simplement de la liste au rechargement — `ApiService.utilisateurs(actifsSeuls = true)` ne pouvait pas distinguer « argument omis » de « `undefined` explicite » (les deux déclenchent la valeur par défaut en JS/TS), donc les 4 appelants qui passaient `utilisateurs(undefined)` pour dire « tous les comptes » obtenaient quand même le filtre « actifs seulement ». Corrigé : le paramètre devient `boolean | null`, `null` servant de sentinel explicite sans ambiguïté. Les 5 appelants sans argument (sélecteurs de responsable) restent inchangés (filtrage aux comptes actifs toujours voulu là).
  - **Bug 2** (confirme la note ci-dessus sur l'absence de zone.js) : même sans rechargement, le bouton ne changeait pas de libellé juste après le clic — `basculerActif`/`valider`/`changerRole` mutaient directement l'objet déjà affiché depuis le callback d'un appel HTTP asynchrone, sans jamais réécrire le signal `utilisateurs`. Corrigé via une nouvelle méthode `majUtilisateurLocal()` qui passe par `.update()`.
  - **Gap systémique signalé, non traité** : le même anti-patron existe ailleurs dans ce fichier (`basculerPermission` sur la matrice, impact visuel plus faible) et potentiellement dans d'autres composants de l'application — jamais audité en totalité, à traiter au cas par cas.
  - Vérifié : reproduit AVANT correctif (bouton figé, personne disparue après rechargement), confirmé résolu APRÈS pour 2 comptes testés l'un après l'autre (mise à jour immédiate + persistance après rechargement). 184/184 tests backend (aucun changement backend). Déployé — frontend seul, `juria-web-00065-t6b`. Détail dans `HISTORY.md`.
- **Renommage « Cockpit » → « Tableau de bord » (04/09/2026)** : demande explicite de l'utilisateur après discussion de terminologie — « Cockpit » (nom d'origine, hérité tel quel de la démo HTML de spécification) est un terme de contrôle de gestion/BI, moins immédiatement clair pour tout le personnel (assistantes, comptables) qu'un avocat familier de la métaphore de pilotage. « Tableau de bord » retenu : cohérent avec le reste de la nomenclature française de JURIA (pas d'anglicisme comme « Dashboard »), et c'est le terme employé par les 3 logiciels français du secteur déjà étudiés dans le benchmark du 03/09/2026 (Secib/Septeo, Kleos, Jarvis Legal). **Renommage d'affichage uniquement** — libellé du menu (`app.component.ts`) et titre de l'écran (`cockpit.component.ts`, `<h1>`) changés ; route (`/cockpit`), nom de fichier/composant (`CockpitComponent`) et commentaires internes du code laissés inchangés (pas de bénéfice à renommer l'identifiant technique, cohérent avec la pratique courante de dissocier libellé UI et nom interne). Build Angular vérifié, aucun changement backend.
- **6 indicateurs de performance ajoutés au Tableau de bord (04/09/2026)** : demande explicite de l'utilisateur (« quels autres éléments de performances... »), proposition faite puis confirmée en totalité — CA du mois + tendance, ancienneté des impayés (5 tranches, 0-30/31-60/61-90/+90 jours), taux de recouvrement (encaissé/facturé du mois), CA par pôle (Conseil vs Contentieux), concentration clients (part du CA que représentent les 5 plus gros clients sur 12 mois — indicateur de risque de dépendance), productivité par collaborateur (valeur FCFA du temps facturé, distinct du taux de réalisation déjà en place qui mesure une proportion d'heures). Toutes gardées par `factures.consulter`, même mécanique tuile+détail+tri que les 9 indicateurs précédents (`GET /api/dashboard/detail/:type` étendu à 6 nouveaux types). 184/184 tests, vérifié avec des données réelles créées pour l'occasion puis en production (8 nouveaux champs présents, 6 routes de détail en `200`, confidentialité reconfirmée avec un compte collaborateur). Déployé — API `juria-00064-xgc`, frontend `juria-web-00067-bhr`. Détail dans `HISTORY.md`.
- **Uniformisation des liens d'action inline sur toute l'application (05/09/2026)** : « peut-on rendre les onglets d'actions plus esthétiques... imbriqués les uns dans les autres ». Confirmé visuellement en production (pas juste supposé) : « Désactiver »/« Réinit. mot de passe » s'affichaient collés sans aucun espace — 17 écrans redéfinissaient chacun localement leur propre `.lien` (tailles/marges incohérentes, la plupart sans marge du tout), aggravé par `preserveWhitespaces:false` (défaut Angular en production) qui retire les espaces blancs entre éléments adjacents du template. Style `.lien` unique centralisé dans `styles.css` (gras, soulignement au survol, séparateur vertical discret automatique entre deux actions adjacentes via `.lien + .lien`) — 15 redéfinitions locales redondantes supprimées, sans toucher au HTML d'aucun écran. 2 exceptions gardées à dessein : `ouverture.component.ts` (lien inséré dans une phrase, contexte différent d'une action de tableau) et `cockpit.component.ts` (bouton « ✕ Fermer », renommé `.fermer` — utilisait `.lien` par erreur de nommage alors qu'il a toujours eu un style de bouton borduré distinct). Vérifié visuellement (Playwright, avant/après sur Accès & permissions et Pièces GED d'une fiche dossier) puis en production sur l'écran exact du constat initial. Aucun changement backend. Déployé — frontend seul, `juria-web-00068-gcv`. Détail dans `HISTORY.md`.
  - **Question de suivi le même jour** (« c'est quoi les boutons `.btn` collés ? ») : audit des 17 écrans utilisant `.btn` (même absence de style global constatée qu'avec `.lien`) — contrairement à `.lien`, les groupes de `.btn` sont presque partout déjà enveloppés dans un conteneur avec espacement prévu (`.actions`/`.upload`/`.btns`/`style="display:flex;gap:Npx"`). **Un seul vrai cas trouvé** : Rôle d'audience, formulaire « Saisir un retour d'audience » (« Enregistrer le retour »/« Annuler » sans conteneur) — corrigé en réutilisant la classe `.actions` déjà présente dans le même fichier, pas de règle CSS globale ajoutée (aurait doublé l'espacement des groupes déjà correctement enveloppés ailleurs). Déployé — `juria-web-00069-h72`.
- **Rétrocessions réservées aux avocats (04/09/2026)** : décision explicite de l'utilisateur — les juristes/stagiaires non-avocats (palier `non_avocat`, 10%) sortent entièrement du dispositif de rétrocession ; associé (30%) et avocat collaborateur/stagiaire/Of Counsel (25%) inchangés. `POST /api/retrocessions` refuse toute nouvelle rétrocession pour un bénéficiaire dont le rôle réel n'est pas un rôle d'avocat, et refuse explicitement la qualité `non_avocat` même pour un avocat. `GET /qualites` ne propose plus ce palier. Le sélecteur de bénéficiaire (`retrocessions.component.ts`) ne liste plus que les avocats. **Rien réécrit rétroactivement** : la valeur `non_avocat` reste dans l'ENUM et `TAUX_DEFAUT` pour l'historique déjà enregistré, seulement bloquée pour l'avenir — même principe que les autres valeurs d'enum devenues obsolètes sans être supprimées (`role_partie.ministere_public`/`partie_civile`, 20/08/2026). 184/184 tests, déployé. Détail dans `HISTORY.md`.
- **Libellés « réclamé par » raccourcis + option « Autre » (31/08/2026)** : 2ᵉ retour utilisateur le même jour — libellés raccourcis au maximum (« Réclamation client » / « Réclamation adverse ») et nouvelle option « Autre » avec précision libre (même patron que `statut_procedure`/`statut_procedure_precision` déjà utilisé sur cet écran). Nouvel `ENUM` value `autre` + colonne `montant_litige_sens_precision`, migration séparée de son propre import (sans usage de `'autre'` dans le même script, évite le piège connu). 3ᵉ occurrence le même jour du piège `--user=juria_app` refusé sur `dossiers`, résolu avec `--user=postgres`. Smoke test fonctionnel + 177/177 tests + vérifié en production. **Déployé le 31/08/2026** — API `juria-00055-bj2`, frontend `juria-web-00056-5nb`.
- **Correctif visuel « réclamé par » (31/08/2026)** : retour utilisateur immédiat sur la fonctionnalité précédente — accolé au montant, le suffixe étirait la cellule flex de l'en-tête (`.dcard .meta` est un `flex`, pas des colonnes égales) et déséquilibrait la rangée. Corrigé exactement dans le sens suggéré par l'utilisateur : nouvelle classe `.montant-sens` (petit, italique, sous le montant, entre parenthèses). Frontend seul. **Déployé et vérifié le 31/08/2026** — `juria-web-00055-fwg`.
- **Qui réclame le montant du litige (31/08/2026)** : suite de l'entrée précédente — nouvel `ENUM sens_montant_litige` (`reclame_par_client`/`reclame_par_partie_adverse`/`indetermine`) et colonne `dossiers.montant_litige_sens` (défaut `indetermine`, aucune migration de données nécessaire). Sélecteur « Réclamé par » ajouté à l'ouverture (Étape 2) et à « Modifier la fiche » ; suffixe affiché en en-tête seulement si renseigné. La « nature » de la somme (l'autre moitié de la demande initiale) reste portée par le champ « Nature / objet » existant, pas de nouveau champ pour ça. Piège de migration déjà connu (`--user=juria_app` refusé sur `dossiers`, résolu avec `--user=postgres`, comme le 21/08/2026 sur `client_pieces_kyc`). Smoke test fonctionnel complet avant déploiement (création/lecture/mise à jour partielle via API locale). 177/177 tests, build OK. **Déployé et vérifié le 31/08/2026** (accord explicite, « vas-y ») — migration appliquée, API `juria-00054-nvr`, frontend `juria-web-00054-t7q`. Vérifié sur un dossier réel puis valeur de test annulée après coup (donnée non factuelle sur un vrai dossier).
- **Date d'ouverture affichée + libellé « Montant du litige » (31/08/2026)** : 2 observations utilisateur confirmées réelles — `dossiers.date_ouverture` existait en base depuis toujours mais n'était sélectionnée ni par `GET /api/dossiers` (liste) ni affichée nulle part (ni fiche, ni liste) ; l'en-tête de fiche affichait juste « Montant » alors que le formulaire d'édition disait déjà correctement « Montant du litige ». Corrigé : colonne « Ouvert le » ajoutée à la liste des dossiers et à l'en-tête de la fiche, libellé harmonisé. **Nuance soulevée par l'utilisateur, proposée mais non implémentée** : distinguer qui réclame le montant (client vs partie adverse) et sa nature — nécessiterait une nouvelle colonne (schéma), laissé à sa décision. 177/177 tests, build OK. **Déployé et vérifié le 31/08/2026** — API `juria-00053-zhd` (précédente `juria-00052-5gr`), frontend `juria-web-00053-kbw` (précédente `juria-web-00052-jtq`).
- **Intitulé du dossier suggéré automatiquement depuis l'Étape 2 de l'ouverture (31/08/2026)** : répond au constat utilisateur que l'en-tête de la fiche dossier (`intitule`) provenait tel quel du champ « Intitulé du projet » de l'Étape 1 (contrôle des conflits), sans lien avec la convention du cabinet (« Client c/ Partie adverse » en contentieux). Point d'architecture signalé avant de proposer : le Pôle (Contentieux/Conseil) n'est choisi qu'à l'Étape 2, donc la formule ne peut être figée dès l'Étape 1. Champ « Intitulé du projet » supprimé de l'Étape 1 (aucune utilité fonctionnelle — vérifié que `conflicts.js` ne l'utilise pas pour la recherche de conflit). Nouveau champ « Intitulé du dossier » en Étape 2, suggéré automatiquement (`Client(s) c/ Partie(s) adverse(s)` en contentieux, client seul en conseil, jointure « et » si plusieurs) dès que client/pôle/parties sont connus — reste un champ texte libre, jamais écrasé si modifié manuellement (même garde que `<app-client-picker>`). **Différent de la tentative du 20/08/2026** (composition recalculée à chaque affichage de la fiche, abandonnée le jour même) : ici le calcul n'a lieu qu'une fois à la création ; une fois créé, `intitule` redevient un champ ordinaire, sans re-suggestion en édition (option retenue, l'utilisateur a accepté de tester). Vérifié en production avec un parcours complet réel (client créé à la volée, bascule contentieux/conseil, jointure multi-parties, préservation d'une modification manuelle, fiche finale affichant exactement la formule composée) — dossier de test archivé après vérification. **Déployé et vérifié le 31/08/2026** — frontend seul, `juria-web-00052-jtq`.
- **Regroupement visuel du menu latéral (30/08/2026)** : les 19 entrées de la sidebar, jusque-là une liste plate, sont désormais réparties en 4 groupes avec en-tête (Dossiers & clients, Production, Finance, Cabinet) — Cockpit/Messagerie restent en tête et Mon compte en pied, sans étiquette. Structure reprise d'un échange antérieur de cette session, jamais implémentée jusqu'ici (confirmé par relecture du code, rien dans `HISTORY.md`). Un en-tête de groupe ne s'affiche que si au moins une entrée du bloc reste visible pour le rôle courant (pas d'intitulé flottant sans rien dessous). Vérifié en production (Playwright, sidebar dépliée + réduite). **Déployé et vérifié le 30/08/2026** — frontend seul, `juria-web-00051-sh4`.
- **Vérification visuelle (compte archiviste) — 3 gaps supplémentaires trouvés et corrigés (30/08/2026)** : à la demande de l'utilisateur (« Vérifie visuellement avec un compte archiviste »), premier usage dans ce projet d'un navigateur headless (Playwright via Docker) pour capturer l'écran réel. A révélé 3 éléments d'UI que le recoupement automatisé de la veille (grep « le code apparaît au moins une fois dans le fichier ») avait laissés passer, tous dans les 2 fichiers les plus complexes du balayage : le panneau « Assistant IA » **intégré à la fiche dossier** (`dossier-detail.component.ts`, distinct de l'écran dédié déjà filtré — appelait `ia.resume`/`ia.chronologie` sans garde), le formulaire « Délais & audiences » de la même fiche (doublon non gardé du formulaire équivalent d'Échéancier, `evenements.creer`), et les boutons « Modifier »/« Mettre à jour le statut » de la fiche client (`client-detail.component.ts`, `clients.modifier` gardait déjà une autre section du même fichier mais pas ceux-ci). Relecture complète (pas seulement `grep`) des 2 fichiers + re-vérification des autres fichiers du balayage — rien d'autre trouvé. 177/177 tests, build OK, vérifié visuellement avant/après en production sur le vrai compte de test (captures d'écran). **Déployé et vérifié le 30/08/2026** — révision `juria-web-00050-8dq`.
- **Diagnostic Facturation — objet dédié, « Autre » (client sans dossier), correction avant règlement (05/09/2026)** : suite d'un audit demandé par l'utilisateur sur les provisions/avances d'honoraires et les meilleures pratiques de facturation des cabinets internationaux — **correction importante de l'utilisateur à retenir** : une provision/avance d'honoraires est un paiement partiel au bénéfice du cabinet sur un montant convenu avec le client, **pas** un dépôt CARPA pour un tiers (confusion initiale corrigée). 3 features approuvées ensemble (« Oui, vas-y ») :
  - **`factures.objet`** (nouvelle colonne, optionnelle) : jusque-là seul `dossiers.objet` alimentait l'objet imprimé sur le PDF — acceptée par `POST /api/factures`, priorité dans `facturePdf.js` (`fac.objet || fac.dossier_objet || fac.dossier_intitule`). Écran : champ pré-rempli depuis l'objet du dossier choisi (`GET /api/dossiers` étendu pour exposer `d.objet`, absent de la liste jusqu'ici), reste éditable, jamais recalculé après.
  - **« Autre » (facturer un client sans dossier)** : `POST /api/factures` acceptait déjà `client_id` seul depuis le 17/08/2026 — aucun changement backend, gap purement côté écran (formulaire de saisie manuelle de `facturation.component.ts` n'offrait qu'un `<select>` de dossiers). Option `__autre__` bascule vers `<app-client-picker>`. Le second formulaire (temps + débours) n'a pas cette option — un dossier y est structurellement indispensable.
  - **`PUT /api/factures/:id`** : corrige une facture `emise` (objet/montant_ht/taux_tva/montant_frais/échéance/règlement/mention) tant qu'aucun paiement n'est enregistré (`409` sinon, `404` si absente) — recalcule TVA/TTC/contre-valeur FCFA en conservant le taux de change et les débours déjà figés. Pas de cycle de vie brouillon complet (question posée puis tranchée avec l'utilisateur) : jugé disproportionné pour une simple faute de frappe sur une facture non réglée. Réutilise le palier de permission de `factures.annuler` (même sensibilité). Écran : bouton « Modifier » à côté de « Annuler », édition inline (objet/HT/frais/échéance/mention — TVA et coordonnées de règlement restent modifiables côté API seulement).
  - Vérifié : 191/191 tests (7 nouveaux), build Angular OK. **Déployé et vérifié en production le 05/09/2026** — migration `factures.objet` (`--user=postgres`, piège déjà connu sur cette instance), API `juria-00065-f87`, frontend `juria-web-00070-4f4`. Vérifié avec un compte associé réel : facture sans dossier créée avec objet explicite, corrigée via `PUT` (HT et objet modifiés, TTC recalculé).
  - ⚠️ **Constat incident, non traité dans cette passe** : en cherchant un compte de test, plusieurs comptes réels (`associe_fondateur`, `admin_it`, `admin_general`, `comptable`) se sont révélés nettement en retrait des permissions documentées dans ce fichier (ex. `associe_fondateur` à 24 permissions au lieu des 63 attendues depuis le 29/08/2026) — signalé pour audit dédié dans une prochaine session, pas d'action corrective prise ici (hors sujet de cette passe). Détail dans `HISTORY.md`.
- `DOC/` : `JURIA - Dossier de Specifications Fonctionnelles - MAJ 16.08.2026.docx` (17 modules détaillés, généré à partir de la démo HTML fournie par l'utilisateur).

## Notes pour les futures sessions Claude
- Avant toute action structurante (choix de librairie, changement de stack, décision d'architecture), consigner l'entrée correspondante dans `HISTORY.md`.
- Ce fichier (`CLAUDE.md`) doit être tenu à jour à chaque évolution significative de la stack ou de l'organisation du projet.
